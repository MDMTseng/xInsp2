{
  "name": "raw_thread_crash",
  "description": "qa_fault copy: on arm, spawns a RAW std::thread that null-derefs, escaping the host SEH translator -> minidump + crash report. Used by QF-I7 to count respawns at the cap.",
  "dll": "raw_thread_crash.dll",
  "factory": "xi_plugin_create",
  "has_ui": false,
  "manifest": {
    "params": [],
    "inputs": [],
    "outputs": [
      { "name": "count", "kind": "number", "doc": "Frames processed. Only on the non-crash path." }
    ],
    "exchange": [
      { "command": "arm",    "args": "{}", "result": "current def JSON; next process() hard-crashes the backend" },
      { "command": "disarm", "args": "{}", "result": "current def JSON; process() returns normally" }
    ]
  }
}
