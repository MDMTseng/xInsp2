{
  "name": "buffer_replay",
  "description": "Buffers incoming records (images + metadata) in a ring and re-emits them via emit_record on an exchange command — the plugin-side replacement for the host's old record/replay. Drives the hot-param re-inspect loop.",
  "dll": "buffer_replay.dll",
  "factory": "xi_plugin_create",
  "has_ui": false,
  "manifest": {
    "params": [
      {"name": "capacity", "type": "int", "default": 16, "min": 1, "max": 1024}
    ],
    "inputs":  [{"name": "img", "kind": "image", "doc": "frame to buffer (any key; passed through)"}],
    "outputs": []
  }
}
