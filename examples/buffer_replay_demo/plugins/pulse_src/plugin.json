{
  "name": "pulse_src",
  "description": "Trivial demo source: each tick emits a 16x16 gray frame whose brightness rises with the seq.",
  "dll": "pulse_src.dll",
  "factory": "xi_plugin_create",
  "has_ui": false,
  "manifest": {
    "params": [
      {"name": "period_ms", "type": "int", "default": 50, "min": 10, "max": 10000}
    ],
    "inputs":  [],
    "outputs": [{"name": "img", "kind": "image", "doc": "16x16 gray, brightness = seq*17 mod 256"}]
  }
}
