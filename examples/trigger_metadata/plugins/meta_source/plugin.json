{
  "name": "meta_source",
  "description": "Source that tags every trigger with routing/context metadata via xi::emit_record (ABI v5). The inspect script reads it back with current_trigger().meta().",
  "dll": "meta_source.dll",
  "factory": "xi_plugin_create",
  "has_ui": false,
  "manifest": {
    "params": [
      {"name": "period_ms", "type": "int",    "default": 50,           "min": 10, "max": 10000},
      {"name": "recipe",    "type": "int",    "default": 7,            "min": 0,  "max": 9999},
      {"name": "command",   "type": "string", "default": "inspect_top"}
    ],
    "inputs":  [],
    "outputs": [{"name": "img", "kind": "image", "doc": "16x16 gray, first 8 bytes = uint64 LE seq"}]
  }
}
