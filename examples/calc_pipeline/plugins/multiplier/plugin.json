{
  "name": "multiplier",
  "description": "Multiplies input.value by a configurable factor. No image processing.",
  "dll": "multiplier.dll",
  "factory": "xi_plugin_create",
  "has_ui": false,
  "manifest": {
    "params": [
      { "name": "factor", "default": 1, "doc": "value is multiplied by this (set per instance config)." }
    ],
    "inputs": [
      { "name": "value", "kind": "number", "doc": "The number to multiply. Missing -> 0." }
    ],
    "outputs": [
      { "name": "result", "kind": "number", "doc": "value * factor." }
    ]
  }
}
