{
  "name": "hang_forever",
  "description": "process() sleeps forever to wedge a dispatch thread. Fixture for the FE-E4 PortUnresponsive DESIGN (see qa_race/PLAN.md). NOTE: on its own this does NOT stall the WS accept loop (inspects run on detached threads), so RACE-FE4 is a documented manual stub until a BE poll-stall hook exists.",
  "dll": "hang_forever.dll",
  "factory": "xi_plugin_create",
  "has_ui": false,
  "manifest": {
    "params": [],
    "inputs": [],
    "outputs": [
      { "name": "count", "kind": "number", "doc": "Never emitted — process() never returns." }
    ],
    "exchange": []
  }
}
