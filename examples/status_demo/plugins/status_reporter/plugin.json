{
  "name": "status_reporter",
  "description": "Calls host set_status (via xi::Plugin::status) each process() to demonstrate per-instance status reporting.",
  "dll": "status_reporter.dll",
  "factory": "xi_plugin_create",
  "has_ui": false,
  "manifest": {
    "params": [],
    "inputs": [],
    "outputs": [ { "name": "n", "kind": "number", "doc": "process() call count" } ]
  }
}
