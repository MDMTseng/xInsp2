{
  "name": "burst_source",
  "description": "Bursty image source: every period_ms emits `burst` triggers back-to-back, then idles. Set burst = 3x a group's max_parallel to make the group's lanes all engage at once (group-parallelism test).",
  "dll": "burst_source.dll",
  "factory": "xi_plugin_create",
  "has_ui": false,
  "manifest": {
    "params": [
      {"name": "burst",     "type": "int", "default": 3,    "min": 1, "max": 256},
      {"name": "period_ms", "type": "int", "default": 1000, "min": 10, "max": 10000}
    ],
    "inputs":  [],
    "outputs": [{"name": "img", "kind": "image", "doc": "16x16 gray, first 8 bytes = uint64 LE seq"}]
  }
}
