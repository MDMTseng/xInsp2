# Overview — mental model + architecture in one picture

> **Scope:** the mental model, the architecture in one picture, and the core
> nouns (Record, Image, plugin, script, instance, trigger). Read once, ~20 min.
> **Status:** SKELETON.
> <!-- source: docs/getting-started.md + the conceptual half of docs/architecture.md -->

<!-- TODO P2: port + tighten. Sections to cover:
  - One-paragraph what-it-is (single-machine inspection authoring; FE supervises BE).
  - Architecture in one picture (FE / BE / extension / runner / SDK).
  - The core nouns + how they relate: Image, Record, plugin, script (use()),
    instance, trigger.
  - Data flow of one inspection: trigger → dispatch → script → use(plugin).process → vars.
  - Pointers into guides/ + reference/ + internals/.
-->
