# Run result (not scheduled)

> **Scope:** per-run result record (one per trigger): a signed status code +
> message + provenance; framework fills non-run cases (drop/crash/timeout) so the
> result stream has no silent gaps; feeds HMI verdict/yield/Pareto + the PLC verdict.
> **Status:** SKETCH — not scheduled.
> <!-- source: docs/design/run-result.md -->

<!-- TODO P2: port as-is (forward-looking). -->
