# Production HMI (partial — v1.0 RUN mode built)

> **Scope:** production operator HMI + standalone package export — a recursive
> split-pane SPA dashboard composer, script-computes-data binding, web-component
> cards, vector overlay layers, `dashboard.json`, AOT production bundle. v1.0 RUN
> mode is built under `hmi/`; compose mode + the rest are forward-looking.
> **Status:** PARTIAL — RUN mode shipped, rest not scheduled.
> <!-- source: docs/design/production-hmi.md -->

<!-- TODO P2: port; mark clearly which parts shipped vs not. -->
