# Roadmap & status

> **Scope:** a THIN status + roadmap — what's shipped (a table that LINKS to the
> relevant `reference/` or `internals/` doc, never re-describes), what's in
> flight, and the not-scheduled sketches in this folder. Replaces the old
> `status.md` single-source-of-truth that drifted by restating other docs.
> **Status:** SKELETON.
> <!-- source: docs/status.md (slimmed to status + links) -->

<!-- TODO P2: build the shipped table as links-only; decision log; pointers to
  the roadmap sketches below. A sketch graduates OUT of this folder into
  internals/ when it ships. -->
