# Linux / cross-platform port (not scheduled)

> **Scope:** cross-platform port (Linux / ARM / macOS): Windows-only inventory,
> effort + phasing estimates, the runtime-compile / AOT-bundle strategy, and the
> going-forward rule that new code stays cross-platform-friendly.
> **Status:** SKETCH — not scheduled (revisit after a stable Windows release).
> <!-- source: docs/design/linux-port.md -->

<!-- TODO P2: port as-is (forward-looking). -->
