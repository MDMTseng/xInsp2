# WebSocket protocol

> **Scope:** the WS wire format — commands, replies, events, and binary preview
> frames; the protocol version vs the C `XI_ABI_VERSION` distinction.
> **Status:** SKELETON — `docs/protocol.md` is already a complete, current
> command-contract reference. **P2 plan: no rewrite.** At cutover, `git mv
> docs/protocol.md` here, tighten lightly, and verify the command list against
> `service_main.cpp`. (Listed here so the index shape is validated.)
> <!-- source: docs/protocol.md -->

<!-- TODO P3 (cutover): git mv + light tighten + verify command list. -->
