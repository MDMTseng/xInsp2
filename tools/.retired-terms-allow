# retired-terms allow-list — EXPLICIT opt-outs from tools/check_retired_terms.py
#
# Each line:  <relpath>[::<substr>]: reason
#   <relpath>            silences EVERY retired-term hit in that file.
#   <relpath>::<substr>  silences only hits on lines CONTAINING <substr>.
# The reason is mandatory and the whole point: it records WHY a retired token is
# allowed to survive here (a compat appendix that documents the legacy path on
# purpose, an archived page, or extension runtime logic that parses user VAR(...)
# tokens for the graph view) instead of leaving a silent gap. If you add a NEW
# developer-facing example, use the CURRENT API — do not add it here.
#
# The checker flags a stale rule (one that matched nothing this run), so prune
# this file when the underlying legacy content is finally removed.

# --- VS Code extension: the VAR-chip pipeline-graph parser ------------------
# extension.ts contains a graph view that PARSES `VAR(...)`/`EMIT(...)` tokens out
# of the USER's script to draw chips. That is runtime logic, not a blessed
# example — the generated-project TEMPLATE in the same file is already clean.
# Silence ONLY the two graph-parser comment lines (anchored to phrases unique to
# that feature) — NOT any bare "VAR" line, so an injected VAR(...) in the
# generated inspect.cpp TEMPLATE further down the same file is still caught.
vscode-extension/src/extension.ts::is declared and: extension VAR-chip graph-parser comment — runtime logic that reads user VAR()/EMIT() tokens, not an authored example
vscode-extension/src/extension.ts::the script surfaces between them: extension VAR-chip graph-parser comment — runtime logic describing how user VAR()/EMIT() tokens become chips, not an authored example

# --- write-a-script.md: the compatibility appendix + legacy-signature section
# This guide intentionally teaches the CURRENT API first, then documents the
# legacy VAR/EMIT + `xi_inspect_entry(int)` path as clearly-marked compatibility
# material (the "## Appendix: legacy VAR/EMIT" section and the "legacy signature"
# comparison). Kept on purpose so owners of existing scripts can still read it.
docs/guides/write-a-script.md: legacy VAR/EMIT + legacy-entry material is documented here on purpose (compat appendix + legacy-signature comparison), current API taught first

# --- reference/onboarding prose that DOCUMENTS the retirement on purpose ------
# These lines mention VAR()/EMIT() precisely to TELL the reader the term is
# retired ("were removed and no longer compile", "REMOVED"). Documenting a
# retired concept is not a copy-paste hazard — the guard's target is examples
# that TEACH it as current. Anchored to the retirement wording so only the
# caveat lines are silenced. (overview.md and data-types.md dropped their
# VAR()/EMIT() token forms in the truth-cut rewrite, so they no longer need a
# rule; ws-protocol.md dropped its token forms in the v12 THE CUT rewrite;
# STUDY-sdk.md moved to docs/archive/ and is off the blessed scan set.)
docs/guides/build-and-run.md::were removed and no: build-and-run note documents that VAR(...)/EMIT(...) were removed and no longer compile, pointing to expose
# data-types.md names TypedPack/TypedPackBuilder/PackSchema exactly to say the
# container "was deleted 2026-07-11" and point at Pack/PackBuilder +
# ScriptTypedPack — retirement notes, not examples. Anchored to that wording.
docs/reference/data-types.md::container was: data-types documents the TypedPack/TypedPackBuilder/PackSchema deletion on purpose ("container was deleted"), teaching Pack/PackBuilder + ScriptTypedPack as current
# c-abi.md's xi_script_set_run_ctx_callbacks row says it "Replaces the retired
# ambient-TLS xi_script_set_run_id (U3) setter" — documenting the retirement.
docs/reference/c-abi.md::retired ambient-TLS: c-abi run_ctx_callbacks row names xi_script_set_run_id precisely to say it is retired and replaced

# --- docs/archive/** --------------------------------------------------------
# (No archived page is in the scanned blessed set today; if archived pages are
# ever added under docs/guides or docs/reference, allow-list them here by path.)
