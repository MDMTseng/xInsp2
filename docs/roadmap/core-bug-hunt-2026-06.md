# Core bug-hunt 2026-06-30 (agent campaign, NOT yet triaged into fixes)

25 confirmed via 12 finders x 3-vote adversarial verify x 3 rounds + completeness critic.
Each survived >=2 of 3 refute lenses (code-trace / reachability / impact). UNVERIFIED by build+run yet.

## Status (2026-06-30)

**FIXED + build/run-verified — the ordered-sink regressions this session's own `emit_record`
work introduced (cluster #15 ≈ #21 ≈ #24, plus #25):**
- `flush_staged_emits_` (service_main.cpp) now **COWs the staged doc only when shared**
  (`DocRegistry::refcount > 1` → private `yyjson_mut_doc_mut_copy`; the rc==1 single-sink path
  still stamps in place, no copy — speed-first) and stamps `$seq` with **put-semantics**
  (`yyjson_mut_obj_remove_str` before the add) so duplicate keys can't accumulate.
- `EmitTurn::wait_turn()` (xi_emit_gate.hpp) now **returns whether it is genuinely this seq's
  turn**; on a cmd:stop wake (every parked seq wakes at once) `run_one_inspection` **skips the
  staged-sink flush** so deliveries can't run out of frame order / concurrently to one sink
  (matches the crash path's "don't push this frame to the PLC"). Wire emits are unchanged.
- Tests: `backend/tests/test_xi_core.cpp` test_emit_gate gains the wait_turn-verdict case;
  `examples/qa_sink_shared_doc/` is a new e2e regression (a two-sink script + a `$seq`-key-counting
  sink). Before the fix sinkB saw **two** `$seq` keys (reproduced by temporarily reverting the
  flush); after, both sinks see exactly one. Full Release build + test_xi_core + test_record green.

**FIXED + build/run-verified — shutdown / lifecycle UAF batch (#2, #3, #6, #7):**
- **#2** watchdog thread joinable on bind-fail → a `WatchdogJoiner` RAII in main stops+joins
  the watchdog on every early return (bind-fail `return 1`, debug `--hang-*` returns).
- **#7** `~PluginManager` destroys instances after `FreeLibrary` + against a dead `ImagePool`
  → `controlled_shutdown_teardown_` now calls `close_project()` (instances die while the pool
  is alive + DLLs mapped); `~PluginManager` clears `project_.instances`/`inst_state_` BEFORE
  the `FreeLibrary` loop (static-destruction backstop); `~CAbiInstanceAdapter` guards its
  `ImagePool::release_all_for` on `g_image_pool_alive`.
- **#6** lifecycle ops don't drain detached one-shots → `InflightRuns` gains a reversible,
  nestable `pause()/unpause()` (distinct from terminal `begin_shutdown`); `quiesce_dispatch_
  for_lifecycle_op_` now `pause()`s launches + `clear_sink()`s + `drain()`s before any DLL
  unload, and `DispatchPoolGuard` reverses both on scope exit (resume re-installs the sink iff
  one existed; dismiss just unpauses). Covers close/open/recompile/rebuild/commit/discard/export.
- **#3** missing-instance `use()` leaked the `share_out`-reserved DocRegistry ref every call →
  `UseProxy::process` releases it on the deterministic `prc == -1` path (mirrors the JSON-fallback
  release; `-2` crash path left alone). Static-verified against the share_out contract.
- Tests: `backend/tests/test_xi_core.cpp` test_inflight_runs gains the pause/unpause case;
  `examples/qa_lifecycle_teardown/` is a new e2e (bind-fail clean exit / clean shutdown with a
  project / open-close cycle under a free-running source — all assert exit code + zero new
  minidumps). #2 confirmed by temp-revert (unfixed = no clean exit-1 / spurious dump). Release
  build + test_xi_core green.

The remaining 17 reports (themes A/C/E below) are still **untriaged backlog** — researched
solution directions + a subagent assignment/sequencing plan are in
**`core-bug-hunt-2026-06-fix-plan.md`** (clusters: A persistence #1/#4/#5/#8/#14/#16, C plugin
load/reload #9/#13/#18/#20/#22/#23, D-residual record/image #10/#11, E dispatch #12/#17/#19).

## [1] ✅ FIXED (2026-06-30) HIGH (votes 3/3, conf 0.89) — load_project silently drops script-instance configs from a saved recipe (fail-reads-as-pass round-trip)
**File:** backend/src/service_main.cpp:3007-3020
**Also:** backend/src/service_main.cpp:2618-2636 (set_instance_def handler, which DOES have the script fallback); backend/src/service_main.cpp:2564-2601 (list_instances emits script instance defs into the recipe)

**Mechanism:** The recipe save path (cmd:list_instances, lines 2564-2567/2597-2601) serializes SCRIPT instances — name + plugin + full def via g_script.list_instances — into the "instances" array, so a host snapshots script-instance configuration into a recipe/project file. But the restore path in load_project (lines 3007-3020) resolves each instance ONLY through xi::InstanceRegistry::instance().find() (line 3015) — that is the BACKEND process's registry (plugin-manager instances). Script instances live in the script DLL's separate registry and are unreachable from the backend singleton; find() returns nullptr and the def is silently discarded. The restore lacks the g_script.set_instance_def fallback that the interactive set_instance_def handler has (line 2629). Unlike the param restore directly above it — which collects param_warnings and reports a partially-restored recipe so it isn't a clean ok (lines 2972, 2997-3001, 3026-3036) — the instance loop collects no warnings and ignores set_def failures, so the recipe loads as success while every script-instance config was dropped. For a vision system this is a fail-reads-as-pass: the line runs with default thresholds/models while the operator believes the saved recipe was applied.

**PoC:** Open a project with a script-declared xi::Instance. Tune it via set_instance_def. Build a recipe file containing the {"instances":[{"name":...,"def":{...tuned...}}]} from cmd:list_instances output. Restart / reopen so the instance is back at default, then cmd:load_project pointing at that recipe. load_project replies ok with no param_warnings, but get_instance_def shows the instance still at the source default — the tuned def was never applied. Contrast a backend (plugin-manager) instance in the same recipe, which IS restored, proving the script path is the gap.

## [2] ✅ FIXED (2026-06-30) HIGH (votes 3/3, conf 0.88) — Bind-failure early return leaves watchdog std::thread joinable → std::terminate/crash-dump instead of clean exit(1)
**File:** C:/Users/TRS001/Documents/workspace/xInsp/xInsp2/backend/src/service_main.cpp:4046
**Also:** service_main.cpp:369 (global g_watchdog_thread); 3989-3990 (thread started); 4200-4201 (the only join, past the early return); xi_crash_dump.hpp:184/376 (set_terminate handler)

**Mechanism:** The always-on watchdog thread is created at line 3990 (`g_watchdog_thread = std::thread(...)`, with `g_watchdog_run=true` at 3989) BEFORE the listen socket is bound. The bind happens immediately after at 4044: `if (!srv.start(port)) { ...; return 1; }` (line 4046). The ONLY place the watchdog is stopped+joined is the normal epilogue at lines 4200-4201 (`g_watchdog_run=false; if (g_watchdog_thread.joinable()) g_watchdog_thread.join();`), which is past this early return. So when `srv.start(port)` fails (port already in use — a routine condition when an FE supervisor respawns a backend before the old process has released the socket, or two backends are launched), main returns 1 while `g_watchdog_thread` is still running and joinable. `g_watchdog_thread` is a file-scope global std::thread (line 369); at static-destruction its `~thread()` runs with `joinable()==true`, which by the C++ standard invokes `std::terminate()`. The crash-dump subsystem installs a `std::set_terminate` handler (xi_crash_dump.hpp:184/376), so terminate is intercepted and writes a full minidump + crash JSON report. Net effect: a routine port-in-use failure that should be a clean `return 1` instead aborts the process via std::terminate and emits a spurious crash report — wrong exit semantics for the FE supervisor and noise in the crashdumps dir. (The same defect applies to the test-only `--hang-after-ready` return 0 at line 4189 and the autostart `--hang-before-ready` return 0 at 4150, but 4046 is the production path.)

**PoC:** Start one backend bound to a port, then launch a second with the same --port (or have the FE respawn before socket release). The second backend reaches srv.start()→false at 4046 and returns 1. Observe: process does not exit cleanly with code 1 — instead std::terminate fires (joinable g_watchdog_thread destroyed), the set_terminate/crash handler writes a minidump + crashdumps/*.json, and the process aborts. Fix: set g_watchdog_run=false and join (or detach) g_watchdog_thread before every early return after the thread is spawned.

## [3] ✅ FIXED (2026-06-30) HIGH (votes 3/3, conf 0.87) — DocRegistry shared-doc reserved-ref leak when xi::use() targets a missing (or doc-path-crashing) instance
**File:** backend/include/xi/xi_use.hpp:363
**Also:** backend/src/service_main.cpp:115-116, backend/src/service_main.cpp:142, backend/include/xi/xi_use.hpp:346-348, backend/include/xi/xi_record.hpp:207-230

**Mechanism:** The γ-4 cross-ABI doc handshake is unbalanced on the failure paths of xi::use(x).process(rec). UseProxy::process (xi_use.hpp:346-348) hands the input metadata doc across the boundary with input.share_out(doc_retain, doc_release). Per share_out's contract (xi_record.hpp:207-230) the FIRST share takes TWO registry refs: (1) an 'enroll' ref tied to the producer Record's DocBox (freed when the input Record is destroyed via release_box_/host_release), and (2) a separate '+1 RESERVED for the adopting side' (xi_record.hpp:220-228). The reserved ref MUST be consumed by the consumer (adopt_shared) OR explicitly doc_release'd if the consumer takes the JSON path. The host honors this ONLY on the JSON-fallback branch: use_process_inline_ does DocRegistry::release((yyjson_mut_doc*)input_doc) at service_main.cpp:142. But when the instance name is not found, use_process_inline_ returns -1 at service_main.cpp:115-116 BEFORE ever touching input_doc, so the reserved ref is neither adopted nor released. Back in UseProxy::process the prc<0 handler (xi_use.hpp:363-369) frees `output` and returns an empty provenance-tagged Record but never releases in_doc either. Net: the reserved DocRegistry ref is orphaned and the host-owned yyjson_mut_doc (plus its DocChunkPool-backed allocation) is never freed. The enroll ref alone reaching 0 only triggers ONE host_release, leaving the registry rc permanently >0. The same gap exists on the prc==-2 doc-input crash path if the plugin faulted before its in_rec Record adopted/unwound.

**PoC:** On a default v4+ host (DocRegistry + doc_retain/doc_release wired by ImagePool::make_host_api, xi_image_pool.hpp:412-416), run a script that does `xi::use("no_such_instance").process(rec)` where rec carries metadata (any .set(...) field, so input.share_out returns non-null). Each call leaks one DocRegistry entry + the doc's pooled chunks; xi::DocRegistry::instance() size grows by 1 per call. Under continuous dispatch this is a per-frame unbounded leak (realistic trigger: an instance the script references was renamed/removed at runtime, or a typo'd use() name — warn_use_miss_ fires once but the doc keeps leaking every frame). Confirm by asserting DocRegistry refcount/size stays flat across N such calls; it instead grows by N.

## [4] HIGH (votes 3/3, conf 0.84) — Malformed-but-nonempty project.json loads with defaults, then first save permanently clobbers other-writer keys (params / auto_respawn / watchdog_ms) — even into canonical via commit — ✅ FIXED 2026-06-30
**FIX:** Quarantine-over-lenient-resume. New `PluginManager::project_json_malformed_` flag set when project.json exists-but-fails-to-parse at open. (1) Open stays UP but degraded, warns "opened READ-ONLY/degraded; saves blocked", and copies the original bytes verbatim to `project.json.corrupt-<ts>` (portable name via `xi::wall_ms()`). (2) `save_project_locked` refuses the full rebuild while the flag is set (returns false before any atomic_write) — so instance CRUD never clobbers extension-owned keys or the recoverable bytes; working-copy commit then mirrors the intact original to canonical instead of propagating defaults. Flag clears ONLY on a fresh successful open of a valid file. Verified: Release + test_xi_core/test_record green; new e2e `examples/qa_corrupt_project_json/` (trailing-comma project.json + params/auto_respawn/watchdog_ms, plain + working_copy:true; open→create_instance→commit, assert keys survive + `.corrupt-<ts>` present — PASS; temp-revert showed all 3 keys clobbered + loss mirrored to canonical, proving teeth).
**File:** backend/include/xi/xi_plugin_manager.hpp:2192-2196
**Also:** backend/include/xi/xi_plugin_manager.hpp:1490-1504 (malformed→defaults), :1735 (open returns true), :2211-2237 (merge allowlist + return), :2337-2341 (save), :1183 (commit mirror)

**Mechanism:** Lenient-parse-then-clobber data loss. (1) open_project parses project.json with yyjson at :1337; on ANY syntax error yyjson_read returns null → project_json_malformed=true (:1492), the loader installs DEFAULT parallelism/groups/runtime, pushes a warning, and RETURNS true (:1735) — the project is treated as fully open, not refused. (2) The VS Code extension owns top-level keys the backend does not emit — `params` (tuned slider values), `auto_respawn`, `watchdog_ms` (comment at :2179-2210 documents this). save_project_locked (:2240) rebuilds project.json purely from in-memory state; its kOwned allowlist (:2211) emits none of those keys, so they only survive via merge_unknown_top_keys_ (:2337). (3) merge_unknown_top_keys_ reads the existing on-disk file and yyjson_read's it again (:2193). For the SAME corrupt file od==null, so the `if (od && nd)` carry-over block (:2196-2234) is skipped entirely and the function returns fresh_json UNCHANGED (:2195/:2237). (4) atomic_write (:2341) then replaces project.json with the defaults-only file. Net: a single trailing comma / truncation in project.json causes the next instance CRUD (create_instance :1840, remove_instance :1881, rename_instance :1997 all call save_project_locked) to silently and permanently overwrite the user's tuned params + auto_respawn + watchdog config + the (hand-recoverable) original bytes with a defaults file. In working_copy mode this clobbers scratch/project.json, and commit_working_copy's mirror_tree (:1183) then copies the defaults file onto the canonical project — so the transactional safety net propagates the loss to canonical rather than containing it.

**PoC:** Create <proj>/project.json containing valid `"params":[{"name":"thresh","value":42}]` and `"auto_respawn":true` plus ONE syntax error (e.g. a trailing comma before the closing brace). cmd:open_project (optionally working_copy:true) → returns ok with an 'loaded with defaults' warning. cmd:create_instance any instance (or rename/remove). Re-read project.json: `params` and `auto_respawn` are gone, replaced by a defaults-only file; parallelism/groups reset to defaults. With working_copy, follow with commit_working_copy and observe the canonical project.json is now the defaults file too. Expected: open should refuse / quarantine an unparseable-nonempty project.json (or block destructive rebuild-saves and preserve the raw bytes) instead of resuming from corrupt state and overwriting.

## [5] HIGH (votes 3/3, conf 0.81) — Working-copy seed ignores copy_tree_excluding() failure → torn scratch presented as authoritative → commit prunes canonical files (silent data loss)
**File:** backend/include/xi/xi_plugin_manager.hpp:1257
**Also:** backend/include/xi/xi_working_copy.hpp:64-94, xi_plugin_manager.hpp:1183 (mirror_tree commit), xi_working_copy.hpp:96-124 (prune)

**Mechanism:** open_project(working_copy=true) seeds a fresh scratch via `xi::wc::copy_tree_excluding(canon, scratch)` at line 1257 and DISCARDS its bool return. copy_tree_excluding is explicitly documented (xi_working_copy.hpp:63-65) to return false on any directory-create/file-copy failure (disk full, read-only dest, a locked/in-use file, a transient IO error) precisely because 'a swallowed error here ... would leave a torn tree while the caller reports success — see commit_working_copy'. The only post-seed guard is `if (!exists(folder/project.json)) return false` (line 1270), which catches a torn seed ONLY when project.json itself failed to copy. project.json is tiny and copied early; a LARGE asset or a deeper instances/<x>/instance.json that fails (e.g. disk fills mid-copy, or a file is momentarily locked) slips through: project.json exists, the code logs 'seeded ... from project', sets canonical_path_, and rebases the project onto an INCOMPLETE scratch. The user then edits this scratch believing it is a faithful copy. On commit_working_copy() (line 1183) mirror_tree(scratch, canonical) does adds/overwrites THEN PRUNES every canonical entry that has no scratch counterpart (xi_working_copy.hpp:108-122). Files that silently failed to seed into the scratch are therefore DELETED from the canonical project on commit — a lossless-looking working-copy round trip permanently destroys real project data. Note also that on the next open the resume branch (line 1254 else) sees project.json present and 'resumes' the torn scratch, cementing it as the source of truth.

**PoC:** 1) Create a project with project.json plus a sizeable file under instances/foo/ (e.g. a large asset, or instances/foo/instance.json). 2) Constrain the destination so the small files copy but a later/larger one fails: fill the disk to leave only a few KB free, or hold an exclusive lock (e.g. another handle with no FILE_SHARE) on instances/foo/instance.json so copy_file fails while project.json succeeds. 3) open_project(folder, working_copy=true): observe '[xinsp2] working copy: seeded ...' logged and the call succeed despite copy_tree_excluding returning false. 4) commit_working_copy(): mirror_tree prunes the un-seeded file from the canonical tree. Result: canonical/instances/foo/instance.json (or the large asset) is gone, with no error ever surfaced. Fix: capture the return and on false remove_all(scratch) + return false (abort the open) the same way commit_working_copy honours mirror_tree's result.

## [6] ✅ FIXED (2026-06-30) HIGH (votes 3/3, conf 0.8) — close_project / lifecycle ops never drain detached one-shot inspects → UAF on plugin DLL unload
**File:** backend/src/service_main.cpp:3288-3304 (close_project handler); 1759-1781 (quiesce_dispatch_for_lifecycle_op_)
**Also:** backend/src/service_main.cpp:1631-1645 (dispatch_one_shot_), 1684-1700 (install_trigger_sink_), 1672 (controlled_shutdown_teardown_ uses drain); backend/include/xi/xi_plugin_manager.hpp:1116-1145 (close_project FreeLibrary); backend/include/xi/xi_inflight_runs.hpp:8-12

**Mechanism:** Lifecycle teardown ops that FreeLibrary plugin DLLs (close_project, discard_working_copy, export_project_plugin, etc.) gate in-flight detached runs ONLY via quiesce_dispatch_for_lifecycle_op_, whose entire detached-run synchronization is a single `{ std::lock_guard<std::mutex> lk(g_run_mu); }` (service_main.cpp:1779). xi_inflight_runs.hpp:8-12 explicitly documents this as insufficient ('only waits for a thread that already HOLDS the lock; one detached-but-not-yet-locked would slip past') — which is why process teardown uses g_inflight.drain() (controlled_shutdown_teardown_ at 1672). close_project does NOT call drain(). The quiesce helper's justifying comment (1774-1779) claims 'New runs can't start meanwhile — one-shot dispatch is on this same (poll) thread', but that is false: one-shot dispatch runs on a SOURCE PLUGIN'S OWN EMIT THREAD via the bus sink (install_trigger_sink_ → dispatch_one_shot_ → g_inflight.launch when !g_continuous, lines 1695-1696, 1637). dispatch_one_shot_ is gated only by InflightRuns.shutting_ (begin_shutdown), which close_project never sets. Sequence on a running (continuous) project: (1) quiesce calls stop_dispatch_pool_ which sets g_continuous=false and joins lane workers, acquires the free g_run_mu, returns; (2) a still-alive camera/source instance emits on its thread → sink fires → since g_continuous is now false it takes the dispatch_one_shot_ branch → launches a detached inspect that acquires the now-free g_run_mu and runs run_one_inspection → the script's inspect() calls xi::use(<plugin>).process(); (3) close_project (service_main.cpp:3300-3302) clears the sink TOO LATE and calls g_plugin_mgr.close_project(), which clears instances then FreeLibrary's every plugin DLL (xi_plugin_manager.hpp:1139); (4) the detached inspect calls adapter->process() → process_fn_, a code pointer into the just-unloaded module → use-after-unload crash. The same applies when the detached thread later drops its instance shared_ptr: ~CAbiInstanceAdapter calls destroy_fn_ into the unloaded DLL. open_project/recompile/rebuild/export share the structural gap (sink stays installed across the plugin-DLL FreeLibrary, no drain).

**PoC:** Load a project whose source plugin runs a free-running emit thread (e.g. frame_source/burst_source emitting every few ms) and whose inspect() calls use(<any plugin>).process(); cmd:start (continuous). From a client, send close_project while the source keeps emitting. Repeat in a loop (harness_open_close_cycle.py style). Intermittently the backend SEGVs inside a plugin process_fn_/destroy_fn_ called after FreeLibrary. Deterministic reproduction: add a sleep between quiesce return and clear_sink in the close_project handler and a one-shot that sleeps in inspect — the FreeLibrary then overlaps the running inspect. Fix mirrors controlled_shutdown_teardown_: clear_sink BEFORE quiesce and g_inflight.drain() before g_plugin_mgr.close_project().

## [7] ✅ FIXED (2026-06-30) HIGH (votes 3/3, conf 0.8) — ~PluginManager at process exit destroys instances AFTER FreeLibrary and against an already-destroyed ImagePool singleton (double use-after-destroy on clean shutdown)
**File:** backend/include/xi/xi_plugin_manager.hpp:85-96
**Also:** backend/src/service_main.cpp:1661-1677 (controlled_shutdown_teardown_, no close_project); backend/src/service_main.cpp:4208-4211 (return 0 → static dtors); backend/include/xi/xi_cabi_adapter.hpp:177-188 (~CAbiInstanceAdapter); backend/include/xi/xi_image_pool.hpp:85-91,241-256; backend/include/xi/xi_plugin_manager.hpp:1116-1145 (close_project, the correct order)

**Mechanism:** The normal shutdown path never closes the open project. controlled_shutdown_teardown_() (service_main.cpp:1661) stops the dispatch pool, clears/resets the bus, unloads the SCRIPT, and nulls g_srv_for_bp — but it does NOT call g_plugin_mgr.close_project(). close_project() is reachable ONLY via the explicit cmd:close_project handler (service_main.cpp:3302); cmd:shutdown (3963-3969) and the g_should_exit epilogue (4208) both skip it. So on a clean cmd:shutdown (or any g_should_exit flip) with a project open, main returns 0 (service_main.cpp:4211) and static destructors run with project_.instances still populated.

~PluginManager (xi_plugin_manager.hpp:85) FreeLibrary's EVERY plugin DLL in its body (lines 90-95), and only AFTER the destructor body completes does its project_ member get destroyed — which drops the last shared_ptr<InstanceBase> refs (InstanceInfo.instance, xi_project_model.hpp:45) and runs ~CAbiInstanceAdapter. That destructor (xi_cabi_adapter.hpp:178) calls destroy_fn_(inst_), a pointer GetProcAddress'd from the now-unmapped DLL — a call into freed code → access violation. This is the EXACT inversion of the order close_project() deliberately uses (xi_plugin_manager.hpp:1122-1142: clear instances FIRST, then FreeLibrary; the comment there documents the SEGV this avoids). The adapter holds no own LoadLibrary ref (dll_ is just the passed HMODULE), so FreeLibrary genuinely unmaps it.

Second, independent UAF on the same path: ~CAbiInstanceAdapter (xi_cabi_adapter.hpp:182) also calls ImagePool::instance().release_all_for(owner_id_). ImagePool is a Meyers singleton constructed lazily at runtime on first image alloc (xi_image_pool.hpp:86), i.e. AFTER g_plugin_mgr's static-init construction (service_main.cpp:1014). Local statics destroyed in reverse completion order ⇒ ImagePool is destroyed BEFORE g_plugin_mgr. So when ~PluginManager → project_ → ~CAbiInstanceAdapter runs, ImagePool::instance() returns a reference to a destroyed object and release_all_for (xi_image_pool.hpp:241) iterates slots_ on freed memory. The g_image_pool_alive guard added for exactly this hazard (xi_image_pool.hpp:58,91) is NOT checked by release_all_for, so it provides no protection here.

Consequence: every clean shutdown with an open project crashes during static destruction — AFTER '[xinsp2] shutdown complete' is printed (4210). The crash-forensics minidump filter installed at startup (crash::install, 3707) is still armed at static-destruction time, so this fabricates a minidump + .json sidecar and an abnormal exit code on what was a graceful shutdown — the FE supervisor reads that as a backend CRASH and triggers a spurious respawn / line-safe alarm.

**PoC:** Open a project that has at least one plugin instance (any source/operator instance → project_.instances non-empty), then send cmd:shutdown (or flip g_should_exit). The backend prints 'shutdown complete', returns 0, and faults in ~PluginManager: either destroy_fn_ called into a FreeLibrary'd module, or release_all_for touching the already-destroyed ImagePool. Observe the access violation + spurious minidump/sidecar produced by the still-installed crash filter. Fix mirrors close_project: have controlled_shutdown_teardown_ call g_plugin_mgr.close_project() (or have ~PluginManager clear project_.instances before the FreeLibrary loop), and gate release_all_for on g_image_pool_alive.

## [8] ✅ FIXED (2026-06-30) HIGH (votes 3/3, conf 0.79) — Project switch leaks prior project's tuned Param values and persistent state into the next project's script
**File:** backend/src/service_main.cpp:2325
**Also:** backend/src/service_main.cpp:69-83 (globals), 2233-2242 (param replay), 2249-2278 (state save/restore), 3202-3287 (open_project), 3288-3304 (close_project), 2156-2168 (state save)

**Mechanism:** g_param_cache (line 83) and g_persistent_state_json/g_persistent_state_schema (lines 69/75) are process-global replay shadows. They are populated on every accepted cmd:set_param (line 2551) and on each reload's state save (lines 2164-2165). They are CLEARED in exactly one place: the unload_script command handler (line 2325 g_param_cache.clear()). Critically, open_project (3202-3287) and close_project (3288-3304) do NOT clear either cache, and neither calls unload_script(g_script) — the inspection-script DLL is independent of the project's plugin DLLs that g_plugin_mgr tears down, so g_script stays loaded across a project switch. Now consider the natural editor flow: open_project A -> compile_and_load A's inspect.cpp -> operator tunes sliders via set_param (g_param_cache fills with A's values; A's xi::state() accumulates) -> open_project B (different recipe) -> compile_and_load B's inspect.cpp. Inside that final compile_and_load: (1) the state-save block (2159-2168) reads g_script — which is STILL A's script (open_project never unloaded it) — so g_persistent_state_json/schema capture A's live state; (2) after the swap, lines 2233-2236 replay EVERY entry of g_param_cache (A's tuned values) into B's script via set_param — any param B also declares by the same name (e.g. "threshold","exposure","min_area", which collide across recipes) is silently overwritten with A's value instead of B's file-scope default; (3) lines 2249-2274 restore A's persistent state into B's script whenever the schema check passes (both unversioned schema==0, or equal versions). Result: project B silently runs inspections with project A's parameter values and/or carried-over cross-frame state. On a vision inspection line this is wrong pass/fail verdicts with no error surfaced. The asymmetry is self-evident: unload_script deliberately clears g_param_cache 'so a future load_project/compile_and_load is free to start clean' (comment at 2322-2324), but the far more common open_project switch path was never given the same reset.

**PoC:** 1) open_project(A); compile_and_load(A/inspect.cpp) where A declares xi::Param<int> thresh{"thresh",50,{0,255}}. 2) set_param("thresh", 200) -> rsp ok; g_param_cache["thresh"]="200". 3) open_project(B); compile_and_load(B/inspect.cpp) where B independently declares xi::Param<int> thresh{"thresh",50,...}. 4) cmd:list_params (or run an inspection) and observe thresh==200 in B, not its default 50 — A's value bled across the project boundary. Same recipe with XI_STATE_SCHEMA unset (schema 0) on both shows A's xi::state() restored into B (g_persistent_state_json never reset). Expected: opening a new project resets the param/state replay shadows the way unload_script does.

## [9] HIGH (votes 3/3, conf 0.79) — External (plugin_dirs, compile:false) plugins are never freed or dropped on close_project/open_project — HMODULE leak + stale entry shadows the next project's same-named plugin with the OLD DLL — ✅ FIXED 2026-06-30 (cluster C)
**FIX (cluster C — one cohesive pass on the plugin load/reload subsystem):**
- **Shared manifest-flag helper** `apply_manifest_flags_(PluginInfo&, folder)` (+ content overload) re-parses reentrant/thread_safe, is_sink (sink/role), json_fallback, factory using the #22-hardened parse. Called from ALL three reload paths — full build `compile_plugin_folders_locked_`, hot recompile `recompile_project_plugin` (#20), cmake reattach `reattach_plugin_from_dll_locked_` (#23) — so a `reentrant:true→false` flip now reaches `effective_cap_` (→1, serialized) on every path, not just full open.
- **#22:** `json_flag_true` substring scan → top-level-only yyjson (bool + quoted-string tolerant; parse-fail→false), so a nested-manifest/description `"reentrant":true` no longer false-positives.
- **#18:** moved `stamp_loaded_dll_` to AFTER the `stale_module` early-return — a pinned-module reattach FAIL no longer poisons the mtime gate, so a later Rebuild retries.
- **#13 + #9:** new `project_loaded_plugins_` set keyed by the MANIFEST name (the key that holds the HMODULE in `plugins_`), populated by every load path incl. compile:false externals; `close_project`/`open_project` teardown both iterate it → FreeLibrary + erase, so a manifest-renamed plugin (#13) and an external (#9) are freed not leaked. The external register-refresh branch now drops+FreeLibrary's the old handle when the folder/dll MOVED instead of keeping a stale handle/c_factory. (Also repaired a latent mis-key: `project_plugin_origin_`/`is_project_plugin`/list-JSON were folder-leaf-keyed vs `plugins_`'s manifest name.)
- Cross-platform: every FreeLibrary tagged `TODO(linux): dlclose`.
- Verified: full Release solution + **17/17 ctest green** incl. new `pm_parse` (#22: nested-only→false, top-level/alias→true; revert→FAIL) + `plugin_teardown` (real prebuilt DLL: manifest `renamed_proj`≠folder + compile:false `renamed_ext` both freed+erased on close; revert→survives=FAIL). #20/#23/#18 reload-path placement + adapter wiring (reentrant→effective_cap_) covered by the shared-parse teeth test + argued by inspection (cl.exe/cmake e2e out of unit scope).

**File:** backend/include/xi/xi_plugin_manager.hpp:203
**Also:** backend/include/xi/xi_plugin_manager.hpp:122-131 (refresh branch keeps stale handle/c_factory), :1136-1143 (close_project frees only origin plugins), :1286-1293 (open_project frees only origin plugins), :1574 (auto-load sets handle for non-origin external)

**Mechanism:** A project.json `plugins` entry resolved against `plugin_dirs` with `compile:false` is registered via `register_plugin_folder_locked_(found.string())` at resolve_external_project_plugins_locked_ (line 203). Unlike `compile:true` externals (which route through compile_plugin_folders_locked_ and record project_plugin_origin_[pname], lines 530/636), a compile:false external is NEVER inserted into project_plugin_origin_. Its DLL handle is later loaded on demand — either by load_plugin() (WS cmd / create_instance) at :1035, or by the open_project instance-loop auto-load at :1574 — setting plugins_[name].handle. 

Both teardown paths only free+erase plugins that are in project_plugin_origin_: close_project iterates `for (auto& [pname,_] : project_plugin_origin_) { ... FreeLibrary; plugins_.erase; }` (:1136-1143) and open_project does the identical loop (:1286-1293). plugins_ itself is never wholesale-cleared (project_ = ProjectInfo{} only resets the project struct, not the manager-wide plugins_ map). So a compile:false external plugin's HMODULE is never released and its plugins_ entry survives into the next project. Switching through N projects that each declare different toolbox externals accumulates N leaked, still-mapped modules.

Worse than a leak: the stale entry causes WRONG CODE to run. If project B declares a `plugins` external with the same manifest name but resolving to a DIFFERENT folder (a different toolbox root in B's plugin_dirs), resolve_external_project_plugins_locked_ → register_plugin_folder_locked_ hits the refresh branch at :122-131: `if (existing != plugins_.end() && existing->second.handle)` it refreshes only description/has_ui/reentrant/is_sink/prebuilt/ui_path/folder_path/manifest_json and KEEPS the existing handle — and never touches c_factory or dll_name. So plugins_["foo"].folder_path now points at B's folder while .handle and .c_factory still belong to A's leftover DLL. Every instance B creates of "foo" is built by A's factory and executes A's code. This is silent: no warning, instances load 'successfully'. If A's DLL had since been unloaded by some other path the factory pointer would be dangling (call into freed module → crash), but in the common case it's simply the wrong inspection logic running.

**PoC:** Two toolboxes T1 and T2 each containing a plugin folder `foo` (foo.dll, manifest name "foo") with different process() behavior. Project A: plugin_dirs=[T1], plugins={foo:{path:foo, compile:false}}, one instance of foo. open_project(A); create/load instance (loads T1/foo.dll, handle set, NOT in project_plugin_origin_). close_project() — observe T1/foo.dll's HMODULE is never FreeLibrary'd and plugins_["foo"] still present with handle!=null. Project B: plugin_dirs=[T2], plugins={foo:{path:foo, compile:false}}, instance of foo. open_project(B): erase-loop skips foo (not in origin map); resolve registers foo, hits refresh branch keeping A's handle/c_factory; instance of foo runs T1's code, not T2's. Confirm by a VAR that differs between T1 and T2 foo. Leak variant: loop open/close over several projects each with a distinct compile:false external and watch loaded-module count grow (Process Explorer / GetModuleHandleEx).

## [10] HIGH (votes 3/3, conf 0.77) — Typed/Field write-through bypasses Record copy-on-write, mutating a frozen cross-ABI shared doc in place (corruption / data race)
**File:** backend/include/xi/xi_types.hpp:146-150 (Typed::set_node_), :63-67 (Field::set_)
**Also:** backend/include/xi/xi_record.hpp:719-738 (cow_ contract) + :241-252 (adopt_shared); backend/include/xi/xi_use.hpp:244-253 (Trigger::meta returns frozen Record); backend/include/xi/xi_use.hpp:375-377 (UseProxy adopt_shared frozen=refcount>1)

**Mechanism:** A Record handed across the ABI via Record::adopt_shared becomes FROZEN with a registry-managed DocBox (box_->host_release set, the other side holds a ref tracked by DocRegistry, not by box_->rc). The whole point of frozen+registry state is isolation: Record::set() -> cow_() (xi_record.hpp:719) detects `host_release != null` and ALWAYS deep-copies into a fresh sole-owned doc before mutating (the comment at :721-723 is explicit: a local rc of 1 says nothing about OTHER sides still reading). The typed-IO layer's write paths do NOT honor this. Typed::set_node_ (xi_types.hpp:146) and Field::set_ (xi_types.hpp:63) call yyjson_mut_obj_remove_key/yyjson_mut_obj_put directly on root_->doc()/node_ with no frozen_ check and no cow_. So mutating a Typed (or Field, via operator[]) whose underlying Record is frozen writes straight into the doc the OTHER side of the ABI still owns and reads. Two concrete reachable sources of a frozen Typed: (1) xi::Trigger::meta() (xi_use.hpp:252) ALWAYS adopt_shares with frozen=true while the host/bus still holds the event's meta_doc ref (released only at release_trigger_event_) — its own doc comment at xi_use.hpp:243 promises 'a mutation copy-on-writes into the script's own doc', but that guarantee holds only for Record::set, not for Typed/Field; (2) UseProxy::process (xi_use.hpp:375) returns a frozen result whenever the producing plugin cached its output doc (doc_refcount>1) — an explicitly supported pattern ('the plugin may keep a cached ref', xi_abi.h:407). Wrapping either in a nominal type (Number/Roi/Region/etc., XI_NOMINAL ctor at xi_types.hpp:165 -> Typed(Record) -> root_=make_shared<Record>(frozen r), node_==root_->json()) and doing roi["x"]=v or tv.set("k",v) then corrupts the host/peer's live doc. If that peer reads concurrently (reentrant plugin, or parallelism.dispatch_threads>1 fan-out), it is an unsynchronized write to a doc another thread reads = data race / UB; otherwise it is silent cross-side state corruption (host trigger metadata, or the producer's cached output).

**PoC:** Script side: `auto m = xi::current_trigger().meta(); xi::Roi r(m); r["x"] = 999;` while the host event's meta_doc is still alive on the dispatch path — observe the host-owned metadata doc (read elsewhere via the same registry pointer) now carries x=999, violating the documented frozen/COW isolation. Or: a plugin that returns its output Record AND keeps a cached doc ref (refcount>1) so UseProxy marks result frozen; the script extracts a Typed from that result and write-throughs a field, then the plugin reads its 'cached' doc next frame and sees the mutation. Run the cached-doc variant under TSan with parallelism.dispatch_threads>1 to surface the read/write race. Contrast: replacing the write with Record::set() on the same frozen Record correctly deep-copies (cow_) and leaves the peer untouched — proving the typed path is the defect.

## [11] HIGH (votes 2/3, conf 0.75) — ImagePool owner-sweep force-frees zero-copy handles still held by another (consumer) instance → UAF / lost frames
**File:** backend/include/xi/xi_image_pool.hpp:241-256
**Also:** backend/include/xi/xi_image_pool.hpp:137 (owner stamped once, never re-stamped); backend/include/xi/xi_cabi_adapter.hpp:182 (dtor sweep) + :238 (process tags creates with producer owner_id); backend/include/xi/xi_abi.hpp:431-437 (record_to_c zero-copy addref forward) + :355-358 (record_from_c adopt); backend/include/xi/xi_image.hpp:96-114 (adopt_pool_handle caches the raw pixel pointer)

**Mechanism:** The ImagePool tags each PoolEntry with the owner_id of the instance whose OwnerGuard was active at create() time (xi_image_pool.hpp:137), and that owner field is set exactly once and NEVER re-stamped (grep confirms the only assignment is line 137). The zero-copy design deliberately SHARES one handle across instances: when producer P returns an image, record_to_c forwards P's existing pool handle with a bare image_addref (xi_abi.hpp:436-437) instead of allocating a new slot, and a downstream consumer Q adopts it via record_from_c → adopt_pool_handle (xi_abi.hpp:357), which addrefs and, crucially, CACHES the raw pixel pointer host->image_data(h) inside its xi::Image's shared_ptr (xi_image.hpp:107-109). So a consumer instance Q that caches frames across calls (a buffer_replay/gathering/accumulator instance — the project's documented multi-frame + replay mechanism) legitimately holds a ref to a PoolEntry whose owner is still P. When P's adapter is destroyed (hot-recompile, remove_instance, rename_instance — all call ~CAbiInstanceAdapter → release_all_for(P) at xi_cabi_adapter.hpp:182), release_all_for force-deletes every entry with owner==P 'regardless of refcount' and `delete e`s the PoolEntry (xi_image_pool.hpp:248-252), ignoring Q's outstanding ref. Q is now holding a dangling xi::Image: its cached data() pointer points into the freed PoolEntry::pixels vector → UAF read on the next replay/use; or, if Q re-resolves through the handle, lookup() fails the generation check (slot freed/reused) → the frame silently vanishes. The design comment 'owner is gone, no remaining consumer is legitimate' is false under the zero-copy cross-instance sharing the pool itself implements.

**PoC:** Project with a source instance `cam` and a `buffer_replay` (or any gathering) instance `buf` that, in process(), stashes the input frame's xi::Image in a member to re-emit on later frames (holds a ref to cam-owned pool entries). Run a few frames so `buf` caches >=1 cam frame. Then hot-recompile or remove/rename `cam`: ~CAbiInstanceAdapter → release_all_for(cam_owner) deletes the cached PoolEntry while `buf` still references it. Next time `buf` reads its cached xi::Image.data()/as_cv_mat(), it reads freed heap (ASan: heap-use-after-free), or the replayed frame is empty. Minimal unit repro: create entry under OwnerGuard(A); adopt_pool_handle into an xi::Image held live; call release_all_for(A); read img.data() → dangling pointer into the deleted PoolEntry.

## [12] HIGH (votes 2/3, conf 0.71) — Watchdog cooperative-cancel flag stays set for the full 1000ms grace, poisoning every fresh inspect dispatched during that window
**File:** backend/src/service_main.cpp:4005-4025
**Also:** backend/src/service_main.cpp:3979-3982 (comment), backend/include/xi/xi_async.hpp:73-82, backend/include/xi/xi_script_support.hpp:280-281

**Mechanism:** On a deadline overrun the watchdog thread does: set_global_cancel(1) (line 4007), then sleep_for(1000ms) (line 4009), then ONLY if no slot is still overran calls set_global_cancel(0) (line 4015). The global cancel flag (xi_async.hpp:73 `global_cancel_flag()`, a single process-global std::atomic<bool>) is therefore held at 1 for the entire 1000ms grace, and `cancellation_requested()` (xi_async.hpp:78-82) returns true purely off that global with NO per-inspect reset at inspect start (run_one_inspection arms a slot at :1173 and never clears the flag). The dispatch pool keeps pulling from its queues independently of the watchdog, so every NEW frame that begins inspecting during the grace window arms a fresh slot, runs its heavy ops, and those ops immediately bail via cancellation_requested()==true. The header comment at :3979-3982 claims only the in-flight frames 'this round' abort and 'healthy workers re-run next tick' — but the next tick is still inside the 1s grace, so healthy heavy frames keep aborting until the flag is finally reset. In the common cooperative-success case (a single marginally-slow frame just over budget) this drops/corrupts ~1 second of subsequent inspections: at 30fps ~30 inspections per slow frame return cancelled/incomplete results (wrong verdicts / missed parts on a vision line). Trivial inspects that never poll cancellation are unaffected, so the poison lands precisely on the expensive ops the system most cares about.

**PoC:** Run continuous mode (cmd:start) at e.g. 30fps with dispatch_threads=1 and --watchdog=100. Use a script whose inspect calls a heavy op that polls xi::cancellation_requested() (e.g. examples/cancel_aware_script.cpp pattern) and that occasionally takes ~150ms. When one frame overruns: observe the watchdog log 'requesting cooperative cancel', then verify that EVERY frame dispatched in the following ~1000ms also returns early/NA (the op sees cancellation_requested()==true) even though those frames are individually fast — they only recover after the watchdog resets the flag at +1000ms. Compare run_result codes across the window to see ~1s of spurious cancellations per single slow frame.

## [13] HIGH (votes 3/3, conf 0.68) — Project plugin with manifest "name" override leaks its HMODULE + survives close_project/open_project as a stale entry — ✅ FIXED 2026-06-30 (cluster C)
**File:** backend/include/xi/xi_plugin_manager.hpp:635
**Also:** backend/include/xi/xi_plugin_manager.hpp:582, 561, 636, 1136-1140, 1286-1290, 1026

**Mechanism:** compile_plugin_folders_locked_ stores the built plugin under its MANIFEST name: `plugins_[pi.name] = std::move(pi)` (line 635), where pi.name is overridden from plugin.json `"name"` (line 582). But every bookkeeping/teardown structure keyed off the FOLDER leaf name `pname`: project_plugin_origin_[pname] (line 636), the drop-prior guard `auto prev = plugins_.find(pname)` (line 561), and the stub-registration `plugins_[pname]` (line 529). close_project tears down project plugins by iterating project_plugin_origin_ (folder names) and doing `plugins_.find(pname)` (lines 1136-1140); open_project does the identical folder-keyed cleanup (lines 1286-1290). When manifest "name" != folder name these keys diverge, so: (a) on close_project/open_project the lookup `plugins_.find(folder)` MISSES the live `plugins_[manifest_name]` entry — its HMODULE is never FreeLibrary'd (module/handle leak) and the PluginInfo (with a now-stale handle + c_factory) survives into the next project; (b) within a session, a second compile of the same plugin frees the wrong key at line 561 (folder), then `plugins_[pi.name] = std::move(pi)` at 635 overwrites the prior manifest-name entry, dropping its old HMODULE with no FreeLibrary. The surviving stale entry is worse than a leak: a subsequent project that references a plugin of that manifest name hits load_plugin's `if (pi.handle) return true; // already loaded` (line 1026) and runs the OLD/already-overwritten DLL's code.

**PoC:** Create plugins/foo/ with plugin.json {"name":"bar","dll":...} + a .cpp. open_project(P1): plugins_["bar"].handle set, project_plugin_origin_["foo"] set. close_project(): loop over origin key "foo" -> plugins_.find("foo") returns end -> the "bar" entry + its HMODULE are never freed/erased. Assert plugins_.count("bar")==1 after close (should be 0) and that the FreeLibrary count is one short. Then open a second project that declares plugin name "bar": load_plugin sees handle!=null and reuses the stale module.

## [14] HIGH (votes 2/3, conf 0.68) — Discard (reopen_fresh_working_copy) destroys the only crash-recovery snapshot when a commit was interrupted → torn canonical becomes unrecoverable
**File:** backend/include/xi/xi_plugin_manager.hpp:1196-1208 (remove_all at :1206)
**Also:** backend/include/xi/xi_plugin_manager.hpp:1218-1240 (open_project roll-forward); backend/src/service_main.cpp:2944-2956 (discard_working_copy handler)

**Mechanism:** open_project's commit roll-forward heals a torn canonical only when BOTH the commit marker AND scratch/project.json still exist (xi_plugin_manager.hpp:1223-1224): it re-runs mirror_tree(scratch, canon) from the intact scratch snapshot. reopen_fresh_working_copy() (the discard / 'throw away my edits' action) does, in order: close_project() (:1204), then std::filesystem::remove_all(<canon>/.xinsp_work) (:1206) — deleting the scratch — then open_project(canon, working_copy=true) (:1207). It NEVER inspects or clears the kCommitMarker. So if a prior commit_working_copy() was interrupted mid-mirror (crash/power loss left the marker present and the canonical tree torn), and the user then clicks Discard instead of reopening, the scratch (the only complete snapshot) is destroyed BEFORE the roll-forward in open_project can fire. Inside the subsequent open_project the roll-forward block now sees exists(scratch/project.json)==false (just removed) and is skipped (:1224); the seed branch (:1254-1259) then re-seeds a fresh scratch by copy_tree_excluding from the *torn* canonical. The torn canonical is never healed and the good snapshot is gone — silent, permanent data corruption/loss. The marker also survives, so a later normal open mirrors the torn-seed scratch back onto the torn canonical (no-op heal) and only then clears the marker.

**PoC:** 1) Open project with working_copy:true, make edits in scratch. 2) Call commit_working_copy but interrupt the process during xi::wc::mirror_tree (e.g. kill mid-copy or simulate a copy_file failure) so kCommitMarker (.xinsp_commit_pending) remains and the canonical is partially overwritten. 3) Restart backend; instead of open_project, issue cmd:discard_working_copy → reopen_fresh_working_copy removes the scratch then re-seeds. 4) Inspect canonical project.json/instances: they reflect the torn mid-commit state, not the pre-commit canonical nor the committed edits, and no recovery copy remains. Compare to the supported path (plain open_project) which heals correctly.

## [15] ✅ FIXED (2026-06-30) HIGH (votes 3/3, conf 0.65) — flush_staged_emits_ mutates a frozen/shared doc in place ($seq via add-not-put): duplicate $seq keys + cross-thread yyjson race when one Record is staged to >=1 sink while another side holds the same doc
**File:** C:/Users/TRS001/Documents/workspace/xInsp/xInsp2/backend/src/service_main.cpp:475-478
**Also:** backend/include/xi/xi_record.hpp:207-229 (share_out freeze contract); backend/include/xi/xi_use.hpp:346-348 (share_out call); backend/src/service_main.cpp:193-224 (stage_sink_emit_ takes the shared doc pointer); backend/vendor/yyjson/yyjson.h:4015-4023 (add_int 'allows duplicated key')

**Mechanism:** stage_sink_emit_ stores `item.rec.meta_doc = (yyjson_mut_doc*)input_doc` — the exact pointer returned by Record::share_out (xi_use.hpp:347). share_out (xi_record.hpp:207-229) sets frozen_=true and documents the doc as 'shared => read-only from here on; a later mutation COWs into a fresh owned doc'. flush_staged_emits_ then writes DIRECTLY into that doc with yyjson_mut_obj_add_int(meta_doc,root,"$seq",run_id) (service_main.cpp:478), bypassing Record/COW entirely. Two distinct consequences: (1) share_out returns the SAME box_->doc pointer for every call on a given Record (xi_record.hpp:112,229). So a script that stages one Record to two sink instances (e.g. a PLC sink + xi::preview::send, both declared sinks) — or to the same sink twice in a loop — produces two StagedEmit items whose meta_doc is the identical pointer D. In the flush loop each item stamps D, and add_int APPENDS (yyjson.h:4017 'allows duplicated key') rather than replacing, so the 2nd..Nth sink receives a doc carrying duplicate "$seq" entries; on serialization that is malformed/non-idempotent JSON (strict consumers reject; yyjson-readers silently take the first). (2) Worse, the write aliases a doc another side may be READING concurrently. The flush runs inside the EmitTurn emission gate (service_main.cpp:1243-1249) while, under parallelism.dispatch_threads>1, the NEXT frame's compute runs fully parallel ('Compute still runs fully parallel; only emission is serialized', service_main.cpp:318-322). The documented gamma-4 pattern is a plugin that adopt_shared()es an input doc and CACHES the pointer across frames for diffing (frozen=true because doc_refcount>1). If that same Record was also staged to a sink, frame N's flush thread executes add_int on D at the very moment frame N+1's worker thread reads the plugin's cached D — a concurrent write/read on a non-thread-safe yyjson_mut_doc => torn read / heap corruption. The root cause is a single contract violation: stamping must either be idempotent (yyjson_mut_obj_put) AND on a doc the host privately owns, never an in-place add on a doc share_out declared frozen and potentially still aliased.

**PoC:** Deterministic dup-key: script inspect() builds one xi::Record rec, calls xi::use("plcSink").process(rec) then xi::use("previewSink").process(rec) (both instances are sinks). Have previewSink's process() serialize its input doc to JSON and log it — observe two "$seq" keys. Race variant: set parallelism.dispatch_threads:2, result_order:"arrival"; write one inline plugin that adopt_shared()es+caches its input doc across frames and reads it every frame, and stage that same rec to a sink; run a continuous burst. Under TSan/ASan (or as intermittent yyjson crashes) you get a data race on the cached doc D between frame N's flush_staged_emits_ add_int and frame N+1's cached-read.

## [16] MED (votes 2/3, conf 0.77) — Script-declared instance defs are lost on every hot-recompile (no replay, unlike params) — ✅ FIXED 2026-06-30
**FIX:** Added `g_instance_def_cache` (name→def-JSON, sibling of `g_param_cache`, under `g_script_mu`, script-instance path only). Populated on accepted `cmd:set_instance_def` (rc==0 script-fallback branch) AND on the load_project recipe-restore success path (so recipe-applied defs also survive a later recompile). Replayed in `compile_and_load` right after the param-replay loop via `g_script.set_instance_def` (best-effort; renamed/deleted no-op). Cleared at all three `g_param_cache.clear()` sites (unload_script + open_project/close_project resets). Backend plugin-manager instances NOT cached — they persist via the singleton `InstanceRegistry` across a script DLL reload. Verified: Release + test_xi_core/test_record green; new e2e `examples/qa_instance_def_recompile/` (tune factor 5→42, hot-recompile, assert still 42 — PASS; temp-revert showed 42→5 = silent revert proven).
**File:** backend/src/service_main.cpp:2233-2242
**Also:** backend/src/service_main.cpp:2604-2637 (set_instance_def handler, no cache); backend/include/xi/xi_script_support.hpp:124-128 (DLL-side thunk)

**Mechanism:** compile_and_load preserves operator-tuned configuration across a reload by replaying g_param_cache into the new DLL via g_script.set_param (lines 2233-2242), exactly because "the new DLL's file-scope default silently overwrites it". Script INSTANCE definitions have the symmetric problem but NO symmetric fix. xi::Instance objects declared in the script live in the script DLL's own InstanceRegistry Meyers singleton (xi_instance.hpp:75 — one instance() per module). They are tuned at runtime through cmd:set_instance_def, which for script instances falls through to g_script.set_instance_def (service_main.cpp:2628-2633). That handler caches NOTHING (no g_instance_cache analogous to g_param_cache at line 2551). On compile_and_load the old DLL is unloaded and the new DLL re-runs its file-scope Instance ctors, re-seeding every instance with its source-declared default def. compile_and_load replays params but never replays instance defs, and instance defs are not part of xi::state() (get_state/set_state only round-trip xi::state(), xi_script_support.hpp:286-300), so the persistent-state path doesn't carry them either. Net: any instance the operator tuned/taught/calibrated via set_instance_def or exchange_instance silently reverts to the source default the moment the script is recompiled — a hot-loop the project explicitly treats as primary (dogfood: hot-tune via set_def/exchange_instance).

**PoC:** Open a project whose inspect.cpp declares an xi::Instance (e.g. a model/threshold holder). cmd:set_instance_def {name, def:{...tuned...}}; confirm via get_instance_def it took. Edit inspect.cpp (any trivial change) and cmd:compile_and_load. get_instance_def now returns the source default, not the tuned def. Compare with a param tuned via set_param across the same recompile, which IS preserved — demonstrating the asymmetry.

## [17] MED (votes 2/3, conf 0.76) — source_last_emit_mono_us_ map grows unbounded — never cleared by reset()/evict_stale()
**File:** backend/include/xi/xi_trigger_bus.hpp:140 (map), :189 reset(), :190 evict_stale(), :212 (decl)
**Also:** backend/src/service_main.cpp:1674,3224,3301 (reset() called at reload/close with stale comment 'release pending_ handles')

**Mechanism:** emit() inserts/updates source_last_emit_mono_us_[source] for every distinct emitting source name (line 140), under mu_. Nothing ever erases from this map: reset() (line 189) and evict_stale() (line 190) are both no-op stubs after the ABI-v6 dispatch collapse, and source_emit_ages_us() only reads it. The lifecycle callers that fire on script reload / project close (service_main.cpp:1674, 3224, 3301) call TriggerBus::reset() expecting it to drop per-source state (the call sites still carry the stale comment 'release pending_/follower_latest_ handles'), but reset() now does nothing. So every distinct source instance name ever seen leaves a permanent {string,int64} entry. Under the project's own hot-iteration workflow — rename_instance, create/remove instance, repeated project close→reopen with renamed sources — the map accumulates dead entries that are never reclaimed for the process lifetime. It is a slow but genuinely unbounded host-side memory leak (bounded only by the total number of distinct source names used over the whole session, not by concurrent sources).

**PoC:** In a long-lived backend: loop N times creating a source instance with a unique name (e.g. 'cam_%d'), have it emit one record, then remove/rename it (or close+reopen the project, which calls TriggerBus::reset()). After N iterations, TriggerBus::instance().source_emit_ages_us().size() == N and keeps growing; reset() does not shrink it. Contrast with the pre-last-emit-tracking design where reset() cleared the bus's per-source/correlation state at every reload.

## [18] MED (votes 2/3, conf 0.76) — Stale-module reattach failure still stamps the change-gate → Rebuild Plugins can never re-reload a plugin stuck on old in-memory code — ✅ FIXED 2026-06-30 (cluster C)
**File:** backend/include/xi/xi_plugin_manager.hpp:393-399
**Also:** backend/include/xi/xi_plugin_manager.hpp:909-913 (mtime gate); :944-979 (Phase C)

**Mechanism:** reattach_plugin_from_dll_locked_ loads the freshly-built DLL, reattaches instances, then unconditionally calls stamp_loaded_dll_(pi, new_dll_path) at :393 — BEFORE the stale_module early-return at :394-398. The stale_module path means FreeLibrary in detach did NOT drop the old module's last ref (a lingering worker thread / pinning dep DLL), so the running code is still OLD, yet the function returns false ('NEW code is NOT active'). rebuild_cmake_plugins marks the item 'failed' (:978) but the PluginInfo.loaded_dll_mtime is now stamped to the just-built DLL's (newest) mtime. On the very next 'Rebuild Plugins' the gate at :910 computes newest_source_mtime(src_dir) <= loaded_dll_mtime → true (no source changed since the build) → the plugin is reported 'unchanged' (:911) and is NEVER detached/rebuilt/reloaded again. So once the pinning thread later exits, there is no Rebuild path that re-maps the new code: the plugin runs stale code indefinitely. Contrast the genuine-fail branches (LoadLibrary :362, ABI :370, factory :379) which all return BEFORE the stamp — only the stale path poisons the gate.

**PoC:** Make a cmake plugin whose instance spawns a detached worker thread that never joins (so FreeLibrary can't unload). Edit a source, Rebuild Plugins → reattach reports 'failed (DLL did not unload)'. Let the worker exit. Click Rebuild Plugins again → item shows 'unchanged', no reload happens; instance keeps executing the previous DLL's code. Only touching a source file (bumping newest_source_mtime past loaded_dll_mtime) or reopening the project forces a reload.

## [19] MED (votes 2/3, conf 0.76) — Single-image emit_record keys the frame by emitter-instance-name, but cmd:run/replay inject and all docs/SDK examples key it "frame" — same script silently gets a null image from a live source
**File:** backend/include/xi/xi_trigger_bus.hpp:154-159
**Also:** backend/src/service_main.cpp:2406 (cmd:run inject keys "frame"); backend/src/service_main.cpp:529-538 (exact-key find, no fallback); sdk/examples/trigger_source/trigger_source.cpp:87-89; sdk/README.md:282; examples/image_sources/plugins/local_image_source/src/plugin.cpp:15

**Mechanism:** In TriggerBus::emit() the per-image key is computed as: `if (image_count > 1 && images[i].key && images[i].key[0]) name = images[i].key; else name = source;` (xi_trigger_bus.hpp:155-159). So for a record that carries exactly ONE image, the record's own key is DISCARDED and the image is stored under `source` (the emitter's instance name). The script reads frames via trigger_image_cb (service_main.cpp:529-532) which does a pure exact-key `g_current_trigger->images.find(source)` with NO fallback — an unmatched key returns XI_IMAGE_NULL. The rest of the core uses the OPPOSITE convention: the cmd:run one-shot inject hardcodes `ev.images["frame"] = h` (service_main.cpp:2406, comment 'read under current_trigger().image("frame")'), and the documented/guaranteed contract everywhere is `current_trigger().image("frame")` (sdk/README.md:282 'key matches what the source emitted'; main README:32/211; docs/guides/write-a-script.md:40; ws-protocol.md:344 which promises replay/inject 'sees them identically to a live run'). The canonical SDK source example emits exactly `xi::Record().image("frame", img)` and its own comment (trigger_source.cpp:87) says the script reads it back as `current_trigger().image("frame")` — but the bus stores that single image under the INSTANCE NAME, so `image("frame")` returns null. Net: an inspect script written to the documented `image("frame")` contract works when the frame arrives via cmd:run/replay/host-inject but silently sees NO image when the same frame arrives from a live single-image source plugin (the bus rekeys it to the instance name). The two core dispatch paths disagree on the keying convention, so no single script can be correct across both frame origins; the failure is silent (empty/wrong inspection result, no error). The repo even ships two contradictory examples: local_image_source:15 documents the instance-name convention (matches the bus) while the SDK trigger_source documents the key convention (matches inject/docs, breaks against the bus).

**PoC:** 1) Build the SDK trigger_source example, add an instance named e.g. 'src1'. 2) Compile an inspect script: `auto t = xi::current_trigger(); VAR(frame, t.image("frame"));` (verbatim from sdk/README.md:282). 3) Let the source emit `Record().image("frame", img)` — observe `frame` VAR is empty/null because the bus stored the image under key 'src1', not 'frame'. 4) Run the IDENTICAL script via `cmd:run --frame some.png`: now `image("frame")` resolves (service_main.cpp:2406 injected key 'frame') and the VAR is populated. Same script, opposite results, depending solely on frame origin. Confirm in code: xi_trigger_bus.hpp:155 (`image_count > 1` gate) vs service_main.cpp:2406 + :531.

## [20] MED (votes 2/3, conf 0.69) — recompile_project_plugin reuses stale PluginInfo manifest fields (reentrant/is_sink/json_fallback) — hot-recompile never re-reads plugin.json — ✅ FIXED 2026-06-30 (cluster C)
**File:** backend/include/xi/xi_plugin_manager.hpp:666
**Also:** backend/include/xi/xi_plugin_manager.hpp:291-292, 587-594, 809; backend/include/xi/xi_cabi_adapter.hpp:280

**Mechanism:** recompile_project_plugin (the save-triggered cl.exe hot-reload, quiesced via service_main.cpp:3357) recompiles the sources and rebuilds adapters via make_adapter_guarded_(pi, ...) which captures pi.reentrant and pi.is_sink into the new CAbiInstanceAdapter (xi_plugin_manager.hpp:291-292). But unlike the full-build path compile_plugin_folders_locked_ (which re-parses plugin.json for reentrant/is_sink/json_fallback/factory/manifest at lines 587-594), recompile_project_plugin NEVER re-reads plugin.json — it reuses the existing pi struct untouched. Consequence: if a developer edits plugin.json `"reentrant": true` -> false (or sink flag) and then iterates by saving a .cpp, the rebuilt instances keep the OLD reentrant=true. effective_cap_() then returns 0 (unlimited) for a plugin that is now actually non-reentrant (xi_cabi_adapter.hpp:280), so the parallel dispatch pool re-enters the same instance's process()/set_def() concurrently -> data race / corruption, with zero diagnostic until a full project reopen. The json_fallback field is likewise stale: the recompile-path ABI gate uses pi.json_fallback (line 809), so a dev who adds "json_fallback":true to recover a yyjson-layout-mismatched rebuild is still refused and loses all instances.

**PoC:** Project plugin with plugin.json reentrant:true and dispatch_threads>1. Edit plugin.json to reentrant:false, then save a .cpp to trigger cmd:recompile_project_plugin. Inspect the rebuilt adapter: reentrant_ is still true so CallScope is a no-op; fire concurrent triggers and observe torn state inside the (now non-reentrant) plugin. Compare against open_project which picks up reentrant:false correctly.

## [21] ✅ FIXED (2026-06-30, = #15) MED (votes 2/3, conf 0.65) — Staged-sink $seq flush mutates a shared registry doc in place, bypassing COW isolation (duplicate keys + corruption of a cached/shared input doc)
**File:** backend/src/service_main.cpp:475-478
**Also:** backend/include/xi/xi_record.hpp:207-229 (share_out returns the same box_->doc on every call); backend/include/xi/xi_abi.h:407-408 (contract: a sink plugin MAY cache the registry-managed input doc by ref across frames)

**Mechanism:** flush_staged_emits_ stamps $seq by mutating the raw metadata doc directly: `yyjson_mut_obj_add_int(it.rec.meta_doc, root, "$seq", run_id)`. This writes through the doc WITHOUT going through Record::cow_(). The whole point of the frozen_/share_out/COW machinery (xi_record.hpp:719 cow_, every mutator calls it first) is that a doc shared across the ABI is read-only and a mutation must copy-on-write into a sole-owned doc so no other side sees the change. flush violates that in two ways. (1) Repeated shares alias the SAME doc pointer: a script that stages the same Record to two ordered-sinks — `use(sinkA).process(rec); use(sinkB).process(rec)` — makes two share_out() calls on the same DocBox; share_out (xi_record.hpp:220-229) reserves a ref but returns the identical `box_->doc` D each time, so stage_sink_emit_ (service_main.cpp:201) stores meta_doc==D in BOTH StagedEmit items. flush then runs add_int($seq) on D twice → D carries two "$seq" members (yyjson `_add_` appends, never dedups), so the JSON delivered to sinkB and serialized onto the wire is malformed/ambiguous (`"$seq":N,...,"$seq":N`). (2) The input doc handed to each sink is the registry-managed D passed by pointer (use_process_inline_ service_main.cpp:131-132 → record_from_c adopt_shared). The ABI contract (xi_abi.h:407-408) explicitly lets the sink plugin keep a cached ref to D across frames. flush's in-place structural mutation of D for item2 then races/corrupts the view sinkA adopted and cached from item1 — exactly the cross-side write COW exists to prevent. The refcount stays balanced (each item: +retain@485, -consume in adopt/json-fallback, -release@507), so this is purely a frozen-doc write-through correctness bug, not a leak.

**PoC:** In an inspection script build one record and emit it to two declared ordered-sink instances with the same object: `auto rec = xi::Record().image("f", img).set("command","weld"); xi::use("plcA").process(rec); xi::use("plcB").process(rec);`. Capture what plcB receives (or dump the staged doc JSON): it contains two "$seq" entries. For the corruption variant, make sinkA a plugin that caches its input doc (Record kept as a member from process()) and reads it on the next frame; under parallel/again-staged frames the cached doc is structurally mutated by a later flush add_int, producing torn reads. Compare against routing each sink its own record (no aliasing) — single $seq, no shared mutation. Fix: COW/clone before stamping (e.g. mutate a per-item copy) or use yyjson_mut_obj_put-style replace on a doc this item solely owns.

## [22] MED (votes 2/3, conf 0.65) — reentrant flag parsed by raw substring scan over the whole plugin.json (incl. nested manifest block) — a false positive silently disables per-instance serialization — ✅ FIXED 2026-06-30 (cluster C)
**File:** backend/include/xi/xi_pm_parse.hpp:30-34, :121-122
**Also:** backend/include/xi/xi_plugin_manager.hpp:587-588; backend/include/xi/xi_cabi_adapter.hpp:280 (effective_cap_)

**Mechanism:** json_flag_true(s,key) does `s.find("\"reentrant\":true")` over the ENTIRE plugin.json text, including the nested `manifest` block (extracted only afterward at xi_pm_parse.hpp:139) and any description/example strings. The sibling key extractor extract_string/detail_find_key was deliberately re-hardened to yyjson top-level-only parsing precisely to close 'key text matched anywhere in a value' (see the D-P1-2 comment at xi_pm_parse.hpp:36-41), but json_flag_true was NOT given the same treatment and still does a flat substring match. If the literal adjacency `"reentrant":true` (or its alias `"thread_safe":true`) appears anywhere in the file — e.g. inside the nested manifest schema, an embedded example config object, or a documentation/description string — pi.reentrant is set true even though the plugin is non-reentrant. That label flows to CAbiInstanceAdapter via make_adapter_guarded_ (xi_plugin_manager.hpp:291-292) and into effective_cap_() (xi_cabi_adapter.hpp:280): for a 'reentrant' plugin the cap becomes max_concurrency_ (0 = UNLIMITED when no cap is set), so CallScope's admission gate is effectively disabled (engaged_=false at xi_cabi_adapter.hpp:292-293). The result: a genuinely non-reentrant plugin's process() (and set_def/exchange) run concurrently on multiple dispatch workers with NO per-instance serialization — the exact data-race the gate exists to prevent (torn instance state / corruption). The misparse is silent: no warning, and it survives reload since the same flat scan runs on every load (also at xi_plugin_manager.hpp:587-588).

**PoC:** Author a normal non-reentrant plugin (no `"reentrant"` top-level key, or `"reentrant": false`) whose plugin.json carries a nested block containing the substring `"reentrant":true` — e.g. a `manifest` example/default object literally written as {... "reentrant":true ...} or a description string mentioning it. Load it, create an instance, run continuous dispatch with parallelism.workers>1. Observe (e.g. via the concurrency_probe pattern in examples/qa_reentrancy) that process() is entered concurrently for the same instance (cur_calls_>1) instead of serialized — confirming the gate was disabled by the mislabel. Contrast with the same plugin whose manifest omits the adjacency: process() is serialized (cur_calls_==1).

## [23] MED (votes 3/3, conf 0.61) — rebuild_cmake_plugins / reattach reuse stale manifest flags (is_sink, reentrant, json_fallback) — editing plugin.json + Rebuild keeps old dispatch semantics — ✅ FIXED 2026-06-30 (cluster C)
**File:** backend/include/xi/xi_plugin_manager.hpp:386-392
**Also:** backend/include/xi/xi_plugin_manager.hpp:280-295 (make_adapter_guarded_ reads pi.reentrant/pi.is_sink); :370 (gate reads pi.json_fallback); :873-981 (rebuild_cmake_plugins)

**Mechanism:** The 'Rebuild Plugins' path (rebuild_cmake_plugins → reattach_plugin_from_dll_locked_) never re-reads plugin.json. reattach reuses the EXISTING PluginInfo: plugin_abi_compatible(..., pi.json_fallback, ...) at :370 and make_adapter_guarded_ at :387 which constructs the adapter with pi.reentrant and pi.is_sink (:292). The only place these manifest flags are parsed is compile_plugin_folders_locked_ (:585-591), which the cmake/rebuild path does not run. So a developer who flips "sink": true (or "reentrant": true, or "json_fallback": true) in plugin.json and clicks Rebuild Plugins gets a rebuilt DLL whose adapter still carries the OLD flags. A newly-marked is_sink plugin keeps running INLINE during inspect instead of being staged into the ordered-emit gate → its side effect (PLC write / preview push) lands out of frame order under parallel dispatch — exactly the corruption is_sink exists to prevent. This is the cmake/reattach sibling of the known recompile_project_plugin staleness (:666); it is a distinct, untouched code path (rebuild_cmake_plugins) and applies to the prebuilt/cmake plugin family the recompile path never handles.

**PoC:** Take a build:cmake plugin without "sink":true; create an instance and observe its process() runs inline. Add "sink": true to its plugin.json, edit a source so the mtime gate fires, run 'Rebuild Plugins'. The reattached adapter is built with pi.is_sink=false (stale), so use(inst).process(rec) still executes inline rather than being staged — verify via run ordering under dispatch_threads>1 that emits are no longer frame-ordered. Same for toggling "reentrant" (concurrency cap wrong) or "json_fallback" (gate uses stale value).

## [24] ✅ FIXED (2026-06-30, = #15) MED (votes 2/3, conf 0.59) — Staged-sink $seq stamp mutates a shared/frozen registry doc in place, corrupting the producer's Record (and duplicating $seq across sinks)
**File:** backend/src/service_main.cpp:475-478
**Also:** backend/src/service_main.cpp:201 (stage_sink_emit_ takes the share_out'd ref as meta_doc); backend/include/xi/xi_record.hpp:207-230 (share_out: marks the producer Record frozen + enrolls the SHARED doc in the registry); backend/include/xi/xi_record.hpp:719-738 (cow_: a registry-managed doc must ALWAYS deep-copy before mutation); backend/include/xi/xi_use.hpp:346-348 (UseProxy::process share_out of the input doc)

**Mechanism:** When a script calls xi::use(<sink>).process(rec), UseProxy::process (xi_use.hpp:346-348) calls rec.share_out(...), which (xi_record.hpp:207-230) (a) sets the producer Record `rec` FROZEN, (b) enrolls the underlying yyjson_mut_doc in the host DocRegistry, and (c) reserves one ref for the consumer — it does NOT copy the doc. The script's `rec` keeps its own DocBox pointing at the SAME yyjson_mut_doc D. stage_sink_emit_ (service_main.cpp:201) stores that reserved ref as item.rec.meta_doc = D. At flush time, flush_staged_emits_ (service_main.cpp:475-478) does `yyjson_mut_obj_add_int(it.rec.meta_doc, root, "$seq", run_id)` — a raw in-place mutation of D. This is exactly the write that the Record COW invariant forbids: xi_record.hpp:719-738 cow_() deep-copies before mutating ANY registry-managed/frozen doc, and set_raw (xi_record.hpp:316-319) even asserts !frozen, precisely because a shared doc is read by another side. The flush bypasses Record entirely and pokes yyjson directly. Consequences: (1) the script's still-live `rec` (which shares D) silently gains a host-injected "$seq" field it never set — corruption of script-visible data behind the script's back; if the script later reads or VAR-emits `rec`, the phantom $seq appears on the wire. (2) When the SAME rec is staged to two sinks (use(plcA).process(rec); use(plcB).process(rec)), both staged items hold the same D pointer; the flush calls yyjson_mut_obj_add_int twice on D (the yyjson 'add' family appends, it does NOT replace), so D ends up with TWO duplicate "$seq" keys and the second sink (and the script's rec) sees a record with a duplicated key — a malformed/ambiguous object for any strict JSON consumer or PLC-side parse. The private-copy branch (service_main.cpp:202-208, JSON data path) is unaffected; the bug is specific to the zero-serialize doc-pointer path, which is the default whenever the script/plugin yyjson layout matches the host (the common case).

**PoC:** Project with one declared ordered sink instance `plc` (plugin.json "sink": true). inspect.cpp:
  auto rec = xi::Record().set("cmd", "weld");
  xi::use("plc").process(rec);          // stages; share_out's rec's doc D (rec is now frozen-shared)
  VAR(seq_seen, rec.get_int("$seq", -1)); // emits -1 at script time, but rec's doc D is the one the host will stamp
Run one inspect. After the staged flush runs (inside run_one_inspection's EmitTurn), D physically contains "$seq":<run_id> even though the script never set it — confirm by having `plc`'s process() log the record it receives AND by reading rec again. Stronger/deterministic variant for the duplicate-key corruption: stage the SAME rec to two sinks:
  auto rec = xi::Record().set("cmd","weld");
  xi::use("plcA").process(rec);
  xi::use("plcB").process(rec);
plcB receives a metadata record whose JSON has two "$seq" entries (yyjson_mut_obj_add_int appended twice to the one shared doc). Fix: COW/clone the doc (or build a private wrapper) before stamping $seq, or stamp via a fresh host-owned copy rather than mutating the share_out'd doc.

## [25] ✅ FIXED (2026-06-30) MED (votes 3/3, conf 0.57) — Ordered-sink staged flush runs out of frame order (and concurrently to the same sink) on cmd:stop / lane teardown
**File:** backend/src/service_main.cpp:1243-1249
**Also:** backend/include/xi/xi_emit_gate.hpp:51-58 (wait_turn wakes on !keep_going); service_main.cpp:1111 (EmitTurn turn(gate, emit_seq, &g_continuous)); service_main.cpp:463-509 (flush_staged_emits_)

**Mechanism:** The whole purpose of staging sink calls (stage_sink_emit_) and flushing them inside the EmitTurn gate is to deliver a sink's side effect (e.g. a comm/PLC write, the documented 'don't push a crashed frame to PLC' guarantee) IN FRAME ORDER even under parallel dispatch (result_order:"arrival", max_parallel>1). run_one_inspection does `turn.wait_turn(); if (inspect_ok) flush_staged_emits_(run_id);` (service_main.cpp:1243-1249). EmitTurn::wait_turn (xi_emit_gate.hpp:55-57) blocks on the predicate `g_->next == seq_ || (keep_going_ && !keep_going_->load())`, and keep_going_ is &g_continuous (service_main.cpp:1111). On cmd:stop, stop_dispatch_pool_ sets g_continuous=false and notify_all's every lane gate (service_main.cpp:1610,1619). EVERY worker parked in wait_turn then wakes immediately REGARDLESS of whether it is actually this frame's turn (gate.next still points at an earlier, still-computing seq). The code at line 1244 then unconditionally runs flush_staged_emits_ — it never distinguishes 'woke for my turn' from 'woke because the lane stopped' (only the later turn.complete() at line 1266 makes that distinction, via `if (g_->next == seq_) ++g_->next`, but flush already ran). Consequence: the staged sink deliveries for the in-flight frames (e.g. seq 5,6,7) are flushed in whatever order their inspects happened to finish, not arrival order, and from multiple worker threads concurrently against the SAME sink instance. The wire run_result/run_finished events are also emitted out of order, but a client can reorder those by run_id — a sink's external side effect (a PLC write, a preview push) CANNOT be reordered, so the ordered-sink invariant is broken exactly for the final frames at stop. A non-reentrant sink survives the concurrency via its per-instance lock, but the delivery order is still nondeterministic; a reentrant sink is entered truly concurrently. The clean intent would be to treat a stop-wake like the inspect-failed branch (drain/drop the staged sends), not deliver them out of order.

**PoC:** Configure a group lane with parallelism.result_order:"arrival" and max_parallel>=4 and a script that does `xi::use("comm_sink").process(rec.set("frame", frame_id))`. Make comm_sink record the order of frame ids it receives in process(). Drive a burst of frames so several inspects are in-flight (give the script a small variable sleep so seq N+1 finishes its inspect() before seq N). While they are in-flight, issue cmd:stop. Observe comm_sink's received-order log: the last few frames arrive out of arrival order (e.g. 7 before 5), and with a reentrant sink, two process() calls overlap. Compare to the same run without a stop, where order is strictly monotonic. Demonstrates the ordered-sink guarantee is violated on teardown.

## Completeness critic
**Uncovered:** WebSocket transport layer (xi_ws_server.hpp) — single-client poll-thread vs worker-thread send_frame concurrency; handshake/auth; fragmentation+partial-read state machine. The hunt covered 'preview-sink' payload gating, never the transport.; Compile/build subprocess management (xi_script_compiler.hpp, xi_cmake_build.hpp) — CreateProcess/_popen handle lifetimes and unbounded WaitForSingleObject(INFINITE). 'script-reload' covered reload logic, not the subprocess.; xi::async engine (xi_async.hpp) — Future/await_all lifetime, SEH-on-worker translation, std::async launch-failure exceptions, CancelToken/global_cancel_flag interactions beyond the one watchdog bug.; Pooled doc allocator thread-exit reclamation (xi_doc_pool.hpp) — per-thread free-list teardown on detached dispatch threads vs in-flight pass-by-pointer docs; yyjson realloc-without-size class lookup.; Graph capture edge reconstruction (xi_graph_capture.hpp) — image-handle identity map correctness when ImagePool recycles freed handles; mid-capture ring eviction.; Atomic file-write primitive durability (xi_atomic_io.hpp) — fsync/FlushFileBuffers ordering, leftover .tmp accumulation on repeated failures, Windows rename-over semantics. Persistence was hunted at the project.json-logic level, not this primitive.; Non-preview sinks (xi_compress_sink.hpp, xi_binary_sink.hpp, xi_log_sink.hpp, xi_status_sink.hpp) — z_stream/deflate finalize on close, FILE handle lifetime, multi-lane concurrent write + shutdown flush ordering.; Crash-dump / SEH path (xi_crash_dump.hpp, xi_seh.hpp) — minidump reentrancy, allocator use during crash, per-worker SEH translator install/teardown.; WS command protocol parsing (xi_protocol.hpp) — command dispatch table + malformed-message bounds. 'abi-surface' covered the plugin C ABI, not the wire-command protocol.; FE supervisor / runner binaries (backend/src/fe_main.cpp, runner_main.cpp) — auto_respawn, child-process handle lifetime, heartbeat windows, BE<->FE crash-history handoff.; Param/config coercion (xi_param.hpp, xi_config_validate.hpp) — type coercion, range clamping, NA-propagation edges at the param boundary.; Instance folder/asset path handling (xi_instance_folders.hpp) and JSON wrapper lifetime/escape (xi_json.hpp, xi_json_escape.hpp).

**Residual suspicions:**
- backend/include/xi/xi_ws_server.hpp:335-345,417,718-743 — worker-thread send vs poll-thread close: send_frame() runs on dispatch WORKER threads under tx_mu_, but the single client_ socket is closed/owned by the poll thread (comment at :743 'poll thread owns the actual close'). The SO_SNDTIMEO mitigation handles a slow client, but tx_mu_ only guards the send — it does not guard client_ itself against being reset to INVALID_SOCK by on_close on the poll thread between a worker's load of client_ and its ::send. Worth tracing whether a worker can ::send on a recycled fd (fd-reuse race the :743 comment claims to avoid is only argued for recv, not the worker-send side).
- backend/include/xi/xi_script_compiler.hpp:438 + xi_cmake_build.hpp _popen — WaitForSingleObject(INFINITE) / blocking _pclose: The compile path calls WaitForSingleObject(pi.hProcess, INFINITE) and _pclose with no timeout. A hung cl.exe/cmake/vcvars (network drive stall, AV lock, child that never closes its stdout pipe) blocks the calling thread forever. If that thread holds g_compile_mu or a lifecycle/quiesce lock, the whole backend wedges with no watchdog coverage over the subprocess. No bug from this hunt addressed subprocess-hang as a hang class.
- backend/include/xi/xi_graph_capture.hpp:96-108 — edge attribution by raw uint64 image handle: producer map keys edges on (uint64_t)image handle. ImagePool recycles handles after free (see xi_image_pool sweep, the subject of a confirmed UAF bug). If a freed handle value is reissued to a different instance within the bounded call buffer, 'last producer wins' can fabricate or misroute an A->B dataflow edge — a correctness (wrong-result) defect in the shipped pipeline-graph feature, distinct from the trace-vs-static concern.
- backend/include/xi/xi_doc_pool.hpp:118-133 — thread_local free-list reclaimed on OWNING THREAD EXIT: Comment states blocks are freed when the owning detached dispatch thread exits. If a pooled yyjson doc (or a block within it) is handed cross-thread via in-process pass-by-pointer (the shipped γ optimization) and the producing detached thread exits while a sink/consumer still references that doc, the free-on-thread-exit walk could reclaim live memory. The cross-thread alloc/free 'lands in freeing thread's list' path (:14) plus thread-exit reclamation is a UAF candidate not traced this round.
- backend/include/xi/xi_async.hpp:199 — std::async(launch::async) launch failure on dispatch path: std::async with launch::async throws std::system_error (EAGAIN/resource) or bad_alloc when the OS can't spawn a thread. Under frame-burst oversubscription (the queued burst-parallelism scenario) this throw originates inside dispatch/use() with no visible catch at the async call site; need to confirm it unwinds safely rather than escaping a noexcept boundary or leaving a half-built CancelToken/Future and aborting.
- xi_compress_sink.hpp / xi_binary_sink.hpp shutdown flush: Sink teardown ordering on cmd:stop / process exit was only examined for the preview sink. A compress sink must emit a deflate trailer on close or the .gz/file is truncated/corrupt; a binary/log sink FILE* not fclosed in the right shutdown phase loses the tail. Multi-lane concurrent writes to one sink + the shutdown drain order is unverified.

**Notes:** Method: enumerated all 58 headers + 5 .cpp, line-counted, and grep-scanned concurrency/lifetime/subprocess primitives in the uncovered files (ws_server, async, doc_pool, graph_capture, atomic_io, inflight_runs, state, script_compiler, cmake_build, sinks). Did not deep-read enough to confirm any new bug — these are scoped, file:line-anchored leads for the next round, deliberately excluding the 25 already-confirmed. Highest-value next targets in order: (1) ws_server worker-send vs poll-close fd race, (2) compiler/cmake subprocess INFINITE-wait hang class, (3) doc_pool thread-exit reclamation vs cross-thread pass-by-pointer docs, (4) graph_capture handle-reuse edge fabrication, (5) compress/binary sink shutdown-flush corruption. The FE/runner binaries (fe_main.cpp, runner_main.cpp) and xi_protocol.hpp command parsing are whole subsystems essentially untouched by a hunt that stayed inside service_main + plugin_manager + the dispatch/record headers.