# new_gen — xInsp3 planning

Planning documents for "xInsp3" — the **ideal form** of the system under the
same philosophy as xInsp2 (speed-first, minimal core,
functionality-as-plugins), incorporating the structural lessons from the
external reviews in [`../ext_review/`](../ext_review/).

The ideal is a north star, not a rewrite commitment: `01`/`02` describe what
the architecture *should* look like free of patch-by-patch thinking; `03`
maps which parts get scheduled onto the real xInsp2, in what order, and which
parts are explicitly greenfield-only.

## Documents

| Order | Document | Content |
|---:|---|---|
| 1 | [`01-xinsp3-architecture.md`](./01-xinsp3-architecture.md) | The main architecture proposal: unchanged spine, the six structural changes (contract-first codegen, core-owned health contract, version identity on every boundary, structural failure boundaries, gates/exemplars as day-1 infrastructure, composed core), system picture, repo layout, what stays out, build order, decision checklist |
| 2 | [`02-plugin-data-contract.md`](./02-plugin-data-contract.md) | Design decision: plugin data stays schemaless `Record` across the ABI; typed structs-across-ABI rejected. Adopted stage 1: hand-written input-builder / output-extractor headers over shared key constants, fail-loud required inputs, schema-version stamp; stage 2 generates the same headers (plus config UI, Python typing, docs) from a published schema |
| 3 | [`03-adoption-map.md`](./03-adoption-map.md) | Maps the ideal onto xInsp2 as schedulable increments: NOW / CARVE / CUTOVER / GREENFIELD-ONLY classification per element, three shippable waves, and what is explicitly not scheduled |
| 4 | [`04-health-contract.md`](./04-health-contract.md) | The core-owned health/state contract as designed and implemented (schema `xi.health/1`): state machine, derived component model, wire surface, quarantine policy, non-goals |
| 5 | [`05-schema-language-spike.md`](./05-schema-language-spike.md) | Schema-language spike findings and recommendation: constrained JSON-Schema subset adopted for the text wire; where JSON Schema fought back (unions, binary framing, int64) and the resolutions |
| 6 | [`06-app-team-migration-plan.md`](./06-app-team-migration-plan.md) | Migration plan for the app team: impact classification (4 action items, behavior fixes, additive surfaces), phases 0–3 (inventory → merge → adopt → `abi` bump cutover), known issues, rollback |
| 7 | [`polaris2/`](./polaris2/00-synthesis.md) | Polaris2 check: four independent architects (no access to new_gen/ext_review) each verified polaris_master and designed their ideal next generation; `00-synthesis.md` compares them against the north star — unanimous validation of contract-first, plus two amendments (immutable tagged arena for per-frame metadata; pure `get_interface`, no monolith struct) and a converged check-findings triage |
| 8 | [`07-uniform-keyed-buffer-plane.md`](./07-uniform-keyed-buffer-plane.md) | Adopted v3 data-plane direction: one frame container of keyed `(type tag, binary buffer)` entries — no image/meta split; everything structured (incl. image/tensor descriptors) encodes as msgpack under a canonical max-width profile — fixed-width numbers give O(1) offset reads and one decoder path (memory ≈ wire ≈ disk); pixels stay raw in aligned pool buffers behind a handle ext type |
| 9 | [`08-polaris2-main-plan.md`](./08-polaris2-main-plan.md) | Implementation plan for `polaris2_main`: strangler round two — wave 0 (deferred polaris2 findings incl. the live-wire conformance test), wave 1 (canonical codec, Frame container, ingress canonicalizer, the GO/NO-GO benchmark), wave 2 (`xi.frame@1` carved pilot on mock_camera→blob_analysis, memory≈wire≈disk proof), wave 3 (contract codegen replacing hand-written accessors) |
| 10 | [`10-pack-migration-scope.md`](./10-pack-migration-scope.md) | Wave-2 exit deliverable (DRAFT for decision): what the frame pilot proved, the proposed migration scope (bilingual batch on polaris2_main; XEX1-v3 default + abi bump on the cutover train), Record's DELETE-behind-gates schedule with P1 and P3 achieved, open maintainer decisions |
| 11 | [`11-command-usage-ledger.md`](./11-command-usage-ledger.md) | Cutover-train decision aid: maps all 55 WS commands (`service_main.cpp` `g_cmd_table`) to their in-tree callers per consumer group, flags the 5 zero-in-tree-caller commands as cutover-train candidates (in-tree-evidence-only caveat — app-team scripts confirm before retiring), plus an in-process findings appendix (dead-code clean bill + KEEP-suspicious `use_grab_cb` retired-stub / `xp::Cmd`) |
| 12 | [`12-pack-parity-matrix.md`](./12-pack-parity-matrix.md) | Gate-P2 instrument: every pattern the examples tree teaches, one row each, classified pack-only-green / blocked-on-named-work / N-A, with evidence pointers; the gate verdict in doc 10 is rendered against this matrix |
| 13 | [`13-replay-file-migration.md`](./13-replay-file-migration.md) | Gate-P3 migration note: XEX1-v3 (per-entry tags) is the one durable record/replay format; per-artifact decisions for pre-v3 files — tagless v2 draft refused (re-record, no guess-laundering converter), Record-era .json/.bmp stay export-only (optional converter parked with the app team on the cutover train), XEX1-v1 is display-only; headline: nothing pre-v3 ever had a replay path, so v3 breaks none |
| 14 | [`14-lib-plugin-capability-plane.md`](./14-lib-plugin-capability-plane.md) | Lib plugins as host-forwarded capability providers — **pilot LANDED on the polaris2 line**: name-only registry with in-pack `$v`/`$probe` versioning, zero-ABI-slot transport (get_interface `xi.cap@1` / `xi.cap.provider@1`), forwarding funnel with the item-14 fault gates + the -5 reentrancy guard, owner-swept registration lifecycle; `xi.imgcodec` shipped as the first lib plugin (dedup-proven encode + decode — read_image_file's designated v12 eviction, net-negative v12 ABI bill); sizing doctrine (heavy only, side-channels off-pattern); xi.infer/compress/calib named-deferred, xi.kv red-flagged; general host-slimming eviction principle; APPENDIX: the resource-handle convention (type-owner lib plugins — heavy objects stay in the owner DLL, packs carry `{type,id,gen,$v}` handle entries; 5 rules: owner alloc/free, immutable, ring/generation lease with never-reused generations -> `stale_handle`, runtime-only + dump materializer, `wrong_type`; `plugins/lut_owner` demo + GPU/VRAM-id variant sketch, NOT scheduled) |
| 15 | [`15-pack-fault-semantics.md`](./15-pack-fault-semantics.md) | Decision doc (U1, landed): the pack-plane error contract — `$fault` as the ONE poison marker (no pack `$na`), the fault schema (`$fault`/`$fault_key`/`$fault_detail` + new reserved `$src`/`$prov`), producer-side provenance stamping at seal (door glue + emit; push stays stamp-free), and the PROPAGATE contract: the host funnel short-circuits fault inputs — the plugin never runs, a new fault pack carries the original reason + `$seq` with the hop appended to the `$prov` chain (the pack mirror of Record NA); closes matrix rows B3/B4, narrows U1's typed-IO leftover out |
| 16 | [`16-script-state-shape.md`](./16-script-state-shape.md) | U2 cut-gate decision (RESOLVED, landed bilingual): post-Record script state is `xi::kv()` — flat typed KV (scalars + nested canonical-mp slots), canonical max-width msgpack boundary bytes over new length-carrying `xi_script_kv_*` exports (the `const char*` state thunks can't carry NUL-bearing msgpack), `XI_KV_SCHEMA` + typed `set_kv_migrate` mirroring the Record choreography; JSON-era state self-seeds script-side during the bilingual window (no host conversion); §What THE CUT deletes lists the exact Record-channel edits. Unrelated to doc 14's red-flagged `xi.kv` blackboard (naming note there) |
| 17 | [`17-pack-ordering-semantics.md`](./17-pack-ordering-semantics.md) | U3 resolution (closes Gate P2 residual #2; flips matrix row C3): the pack-plane ordered-sink contract — delivery ORDER is envelope-carried (staged push flushed inside the emit gate), in-band IDENTITY is producer-stamped `$seq` blessed as the one mechanism (host never stamps sealed packs; `xi::run_id()` bridges the host arrival id), `process()`-on-sink rejected fail-loud (rc −5); rejected alternatives (flush-time splice, door-args) recorded |
| 18 | [`18-stream-chunking-convention.md`](./18-stream-chunking-convention.md) | Streaming via chunking (settled CONVENTION, no host mechanism): a payload too large/long-lived for one sealed pack travels as a sequence of ordinary sealed packs on one lane — reserved `$stream` (i64 id) / `$part` (dense 0-based i64) / `$eof` (bool, last chunk only), producer-declared overlap + ownership bands for exactly-once boundary features, consumer-owned bounded reassembly window (no reorder tolerance on one lane per doc 17), fault semantics (mid-stream `$fault` poisons the stream per doc 15; missing `$eof` → consumer-owned `stream_timeout`); explicitly NOT sub-chunk latency or cross-chunk zero-copy; worked QA example `examples/qa_pack_stream/` |
| 19 | [`19-hardening-backlog.md`](./19-hardening-backlog.md) | Living backlog of quality items surfaced by the example/eviction waves and design discussions — grouped by disposition: (A) schedule-next defects — **section A CLEARED 2026-07-04** (has_source pack-blind, project.json inert instances, QA port-7917 sweep, two known-fail drivers all fixed), (B) v12 cutover-train items (core JPEG encoder eviction, read_image_file slot delete, machine-autoload, ext-type lifetime hooks, stable image identity), (C) low-severity robustness residuals (PackRegistry ledger, cap-funnel metering, orphan codegen emitters), (D) discussion-only designs (mux priority plugin, pinned pool, GPU-island). None block THE CUT |
| 20 | [`20-v12-execution-checklist.md`](./20-v12-execution-checklist.md) | THE CUT cut-day runbook: preconditions, the port-BEFORE-delete dependency spine (Stage A port examples/plugins off Record while bilingual + gate → Stage B the one-commit deletions: ABI v11→12 pure door, delete Record/DocRegistry/COW/monolith + read_image_file slot + core JPEG encoder + xi::state(), XEX1-v3 default + hello.abi 1→2, 5 command retirements → Stage C sweep), verification (8-stage gate + test_abi_freeze re-baseline + joint rig pass), one revert point + the window as the real safety net; blast radius counts (117 example inspect.cpp + 19 plugin files + core headers). Stage B lands ONLY on the agreed train date |
| 25 | [`25-redteam-dataflow-round2.md`](./25-redteam-dataflow-round2.md) | Red-team data-flow findings, rounds 2–5 (benign plugins/users, stress/race only). **Round 2** (4-way audit + independent verification): F1 `get_bin()` fabricates a `{nullptr,N}` span on pool exhaustion → OOB read (P1, FIXED), F2 start/resume window silent drop (P2, FIXED), F3 health-mirror fixed-`.tmp` torn under concurrent fault storm (P2, FIXED), F4 `inspect_tid` single-global false-`abort()` under max_parallel>1 (P3, FIXED per-thread-relational), C1 accessor-vs-sweep UAF (REFUTED — untagged event ref guards it). **Round 2b** (block/rendezvous/semaphore surface): RB1 depth-0 lost-wakeup (P2, FIXED notify_all), RB2 depth-0+max_parallel>1 concurrency breaks 1-in-flight (P2, FIXED single-worker clamp), RB3 stop-wake drop accounting (P3, FIXED), RB4 semaphore-example shared-static (P3, FIXED), RB5 quiesce UAF (REFUTED). **Round 3** (post-fix audit): the 9 round-2 fixes are CLEAN (0 introduced defects); **B1 `prepare()` races `reinit()`'s destroy → UAF (P1, CONFIRMED)** + B2/B3 same reinit-gate gap (prepare + reentrant data plane uncovered by the A1/A2 gate); C3 doc-18 arrival-order premise false under the default `result_order:"completion"` → multi-worker streams silently `stream_gap`-abort (P2). B1/B2/B3 DEFUSED (set_on_fault combo guard); C3/C4 FIXED (doc 18). **Round 3b** (parallel app-team fresh-surface sweep, independently verified): **G1** cap reinit double-free (= B1/B2, RESOLVED by the combo guard); **G2 script `xi::Instance` set_def vs live inspect UAF (P1, CONFIRMED, normal-reachable live-tune — the script-side escape of what F-refuted for backend adapters)**; G3 `parallel_for` drops the inspect cancel-ticket → watchdog-window wrong verdict (P2); G4 `xi::kv()` cross-frame race under max_parallel>1 (P2, doc fix); G5 RT8 writer stop()/join unbounded vs a slow-alive client (P2, one-line ::shutdown-before-join). G2/G3/G5 confirmed-unfixed; G4 doc. **Round 4** (process-lifecycle & fault-infra sweep — independently re-verified in a separate 4-way worktree pass, all H1-H8 CONFIRMED; findings-only): **H1** FE unchecked `WaitForSingleObject` after async `TerminateProcess` → abandoned BE + double-BE on one port/state dir (P2, P1 blast radius); **H2** stale `autostart: ready` marker → boot-gate bypass (P2, chains off H1's zombie holding the `be.log` handle); **H3** concurrent unhandled faults race the ungated minidump writer — non-thread-safe dbghelp + same-second filename collision (P2, lost/hung forensics); **H4** OMP/async workers skip `reserve_fault_stack` (P3); **H5** concurrent `on_fault=reinit` on a non-reentrant instance → redundant double-rebuild + spurious-quarantine window (P3, not covered by the G1 guard); **H6** `CREATE_SUSPENDED`→job-assign orphan window (P3); **H7** hard-exit truncates a concurrent minidump (P3); **H8** unlocked `project_.instances` iteration in the stats/health readers (P3 latent — one off-poll-thread mutation from a P1). Codec + metrics slices refuted-clean. Suggested order H1→H3→H5→rest. **Round 5** (build/load/discovery & staged-egress sweep — independently re-verified, all J1-J9 CONFIRMED; J2+J3 disprove RT5's "remove_instance was the ONLY un-quiesced lifecycle op"; findings-only; doc/comment defects now counted): **J1** script build uses a fixed non-namespaced `work_dir` (`temp/xinsp2`) + per-process version counter → co-resident backends (QA `free_port()` harness, two projects) clobber each other's `inspect_v0.dll` / corrupt the shared PCH (P1); **J2** `cmd_rescan_plugins_` FreeLibrary's a live plugin DLL (moved-manifest branch, un-quiesced) → adapter calls into unmapped code (P1, RT5 class on the discovery path); **J3** `cmd_create_instance_` evicts a live machine-autoload provider without quiescing (P1, autoload-gated); **J4** staged sink `push()` from an off-dispatch thread (parallel_for/async) stages onto a thread_local `g_staged` nothing drains → pack ref leak + silent lost actuation (P2, P1 leak-to-exhaustion under max_parallel>1); **J5** no timeout on any compile subprocess wait → hung toolchain wedges the poll thread (P2); **J6** `scan_plugins` holds `mu_` across a 30s certify subprocess → stalls `instance_group()` on the hot emit path (P2); **J7** compile prune deletes the live script's PDB before the swap (P3); **J8** WS rx cap false-drops a near-max pipelined frame (P3, narrow); **J9** stale comments re-derive the deleted metadata-doc protocol (doc-P3). WS RX framing + (deleted) metadata-doc plane otherwise refuted-clean. Order J2/J3→J1→J4→J5/J6→rest. **J1/J2/J3 (the 3 P1s) FIXED @ `f1fd54e`** — per-PID `work_dir` (unblocks parallel QA) + `quiesce_dispatch_for_lifecycle_op_` wraps on rescan/create-instance; **J1 disk-leak follow-up @ `638dc26`** (reap dead-PID build dirs — the per-PID PCHs had filled the disk); **H1 (round 4) also FIXED @ `e72da07`** (FE confirms backend death before respawn — no double-BE). **ROUNDS 4 & 5 NOW FULLY CLOSED @ `e744e8e`** (5-way parallel implementation, full gate green): H2 (fail-spawn on stale log), H3 (minidump one-shot gate + tid/ms), H4 (reserve_fault_stack on OMP/async workers), H5 (atomic reinit-consume — dispatch + cap-funnel), H6 (job-at-process-creation), H7 (await-dump before hard-exit), H8 (mu_-locked stats/health snapshot); J4 (off-thread push fail-loud), J5 (bounded compile wait), J6 (certify off-lock), J7 (keep N-1 PDB), J8 (rx cap on remainder), J9 (comment refresh). **Round-3b G2/G3/G4/G5 ALSO FIXED @ `d05ed4a`** (G2 quiesce set_instance_def; G3 parallel_for re-installs the inspect ticket+token per worker; G4 kv thread-safety doc correction; G5 RT8 stop() shutdown-before-join). **EVERY finding across rounds 2–5 is now FIXED, DEFUSED, or REFUTED.** (A later dedicated P1 squeeze — rounds 6–7, doc 26 — reopened the sweep and found one more: L1, since fixed. "Closed" was premature at round 5; see row 26.) |
| 26 | [`26-redteam-dataflow-round6.md`](./26-redteam-dataflow-round6.md) | Red-team data-flow findings, rounds 6–7 (benign plugins/users, stress/race only) — reopens the sweep with a **new P1**. **Round 6** (peripheral sync primitives + observability egress, the surfaces rounds 2–5 hadn't swept; 4-way, all re-verified): **no new P1** — K1 image_pool deferred-reclaim walker slot load is `acquire` not `seq_cst` (P3 latent, P1-UAF only on weak arch e.g. Win-on-ARM64; benign x86-TSO but comment overclaims); K2 `ImagePoolOwnerScope` failure dtor omits `sweep_packs_for` → pack-ref leak on seal-then-fail (P3 cold leak); K3 `InflightRuns::drain()` counts 1ms-sleep iterations as wall-ms → Windows ~15.6ms timer stretches "~50s" bound ~10× (P2 doc+behavior); K4 metrics buckets labeled `"le"` (Prometheus cumulative) but implemented non-cumulative (P2 doc/format); K5 snapshot non-atomic multi-counter read (P3); K6 `buckets_[]` missing `{}` init (P3 latent); K7 `source_emit_ages_us` samples clock before lock → negative age (P3); K8 no-sink `emit_pack` drops pack ref via unset releaser, comment overclaims (P3); K9 `graph_capture::set()` clears under lock but flips `on_` outside (P3); K10–K12 graph "full graph" / owner_lock pid_alive Win-vs-Linux asymmetry + pid-reuse ABA (P3 doc). **Round 7 (P1 squeeze — dedicated memory-unsafety pass over the 4 cross-thread custody handoffs): L1 CONFIRMED P1 — `cmd_rename_instance_` (`service_cmd_project.cpp:317-322`) destroys a live producer adapter WITHOUT quiescing dispatch (unlike every sibling), so `~CAbiInstanceAdapter`'s `sweep_packs_for(owner_id_)` (`xi_cabi_adapter.hpp:334`) frees a door-output pack a still-running inspect holds → UAF read of the freed pack + generation-recycled pool slots.** Root cause = the RT5/J2/J3 family (un-quiesced live-adapter teardown), the last reachable escapee; door output is producer-owner-charged (`run_pack_door` OwnerGuard `:414`) and transferred by move without re-retain (`service_sinks.cpp:271` + `xi_use.hpp:855-858`) → single-owner, so the sweep's co-owner fail-safe doesn't spare it. Fix = one-line `quiesce_dispatch_for_lifecycle_op_` wrap (mirrors J2/J3); also a doc defect — the `remove_instance` comment claims rename covered, but only rename-via-reload is, not the in-place path. **L2** (P2/P3, fail-closed) pack push retains owner-tagged (`service_sinks.cpp:179`) but releases untagged → phantom ledger bucket; verified it can NEVER over-release (`reclaim=rc−remaining` protects co-owners regardless), fix = `retain_untagged`. **REFUTED clean:** pack-plane refcount, dispatch custody handoff, PM instance-map lifetime, cabi set_def/reinit/prepare/cap-funnel — all sound. Order K2→K4→K3→K1 for round 6; **L1 (P1) first overall.** L1 is the sweep's final P1 — the "closed" verdict in row 25 predated it. **ALL FIXED @ `02ad9e4`..`c3e7f1f` (2026-07-06, independently re-verified + full gate green): L1 quiesce cmd_rename_instance_ (the last un-quiesced live-adapter teardown, mirrors J2/J3); K1 seq_cst walker slot loads; K2 sweep_packs_for on the failed-ctor scope; K3 wall-time (mono_ms) drain deadline; K4 `le`→`bucket_ms` wire key (+ schema/baseline/fixture/doc); K5/K6 metrics doc+init; K7 clock-after-lock; K8 no-sink null-releaser fail-loud; K9 clear+toggle under one lock; K10-K12 doc corrections; L2 retain_untagged. The rounds-2..7 red-team effort is now genuinely closed.** **Round 8 (regression audit of the 6 round-6/7 fix commits + fresh-surface sweep): the fixes are CLEAN (no regressions — rename quiesce ordering, L2 ledger balance, seq_cst walkers all 3 loops, G3 parallel_for ticket all verified), WS transport 6/6 vectors REFUTED, hot-reload `module_lifetime` VERIFIED SAFE — but M1 REOPENS G4: the round-3b G4 kv-race fix (`d05ed4a`) was DOC-ONLY and its carve-out is WRONG — `max_parallel` is per-group (`service_dispatch.cpp:330,352` sums Σ lanes) but `xi::kv()` is process-global (`xi_kv.hpp:306`) and one global script runs on every lane, so a 2-group project each `max_parallel==1`+no-async (which the doc at `xi_kv.hpp:41-42` calls "safe to skip the lock") still does concurrent `std::map` RMW → torn/UB (P2 doc-defect, reopens G4). M2 (P3) compile prune keep-`ver-1` defeated by the process-global `s_version` shared across script+plugin compiles (`xi_script_compiler.hpp:701,957`) → deletes the still-live outgoing script's `.pdb/.lib` → crash has no line info. M3 (P3 doc) `close_client` notify comment claims the writer re-checks `client_` but it only re-checks the queue predicate (`xi_ws_server.hpp:656`). M4 (P3 load) 2nd-client reject drain runs a 400ms recv-loop on the poll thread (`xi_ws_server.hpp:493-499`). Latent: `toolchain::write_override` writes project.json without `mu_` (safe only while poll-thread-serial). Fix M1 before claiming closure. **ROUND 8 FIXED @ `bfb1993`** (full gate green): M1 → kv contract corrected (only a single-group max_parallel==1 no-async project may skip `kv_mutex()`; multi-group always locks); M2 → prune scans for the real highest same-stem version < ver (not `ver-1`); M3 → close_client comment corrected; M4 → reject-drain non-blocking; latent documented. **NOT re-stamped "closed" — G4 has now been reopened by a fresh angle twice; the sweep is treated as ongoing, not done.** **Round 9 (four net-new surfaces — machine-autoload, stream chunking, msgpack codec, resource-handle lease): no UAF. N1 (P2, CONFIRMED) — machine-autoload evict/reinstate updates `InstanceRegistry` synchronously but the `CapRegistry` sweep rides the pin-deferrable adapter dtor (`xi_pm_load.hpp:70` + `xi_cabi_adapter.hpp:309`), so while an in-flight cap-call pin defers `~M`, `f_cap_call` does lookup→ok/resolve→null → `XI_CAP_ESHAPE` (`xi_cap_abi.hpp:281-285`) on a capability that has a LIVE provider (dropped imgcodec decode/encode → frames fail); self-heals on pin drop. The RT5/L1 lifecycle-teardown family again — as a lost-result, not a UAF (the pin that stops the UAF widens the window). Fix: `evict` should `unregister_all_for(owner)` synchronously, not via the deferred dtor. The 4 UAF hypotheses on this path were REFUTED (shared_ptr pin + reinit-gate + owner-match). N2 (P3 doc) — the resource-handle "five rules" (doc 14:349-373) omit the resolve-time `shared_ptr` pin that is the SOLE reason `lut_owner` is safe (it's only in an impl comment), crediting immutability + gen-lease instead; a future owner following the rules literally but freeing on recycle has a cross-lane UAF, and the doc tells the GPU `cudaFree` variant to copy the lease "verbatim". N3 (P3 robustness, over the benign edge) — `xi_mp.hpp:537` `seen.reserve(e.len)` unbounded from an unchecked map count → wild-alloc abort at the `xi_ingress.hpp:70` "untrusted bytes" door; defeats the codec's own `:452-455` DoS-safety comment; trigger is malformed input so flagged as hardening not benign-race; one-line `min(e.len, remaining())`. N4 (P3 example) — `qa_pack_stream/inspect.cpp:137-140` unbound consumer binds its stream id from a fault → a foreign fault kills the wrong stream (reintroduces F3 contamination in the unbound window). REFUTED clean: lut_owner impl, msgpack offset/payload reads, stream core (no host reassembly), WS fragment reassembly. Order N1→N2→N3→N4. **Round 10 (watchdog/_Exit, prepare/commit, cap shadow-stack, project teardown): a SECOND P1. O2 (P1, CONFIRMED) — `close_project`/`open_project` (`service_cmd_lifecycle.cpp:896/786`) are the only lifecycle handlers that release the detached-launch pause (`g.dismiss()`→`unpause`) BEFORE their FreeLibrary (`:904/:809`); every sibling holds the guard across the op. A source-emit thread that snapshotted the bus sink before the quiesce's clear_sink launches a detached one-shot after the pause drops (`inflight.launch` sees `paused_==0`, `xi_inflight_runs.hpp:73`) → runs concurrently with `close_project`'s FreeLibrary (`xi_pm_project.hpp:95`) → call into an unmapped plugin DLL (use-after-unload; worse than L1 — the shared_ptr pin can't defend an unmapped process_fn_/destroy_fn_). Same RT5/L1 family, pause released one step too early; fix = move dismiss() after the teardown. O1 (P3) — H7's await-dump fix missed the shared `recover_seh_stack_or_die` `_Exit` (`xi_seh.hpp:114`, ~18 untrusted-code call sites) → concurrent minidump truncated + .json sidecar absent (+ 2 doc defects at `xi_crash_dump.hpp:198`/`xi_seh.hpp:102` asserting it holds); structural miss (seh is the lower layer, can't call await_dump without a hook). O3 (P3) — no prepare/commit export-pairing validation (`xi_cabi_adapter.hpp:277`), so a benign-but-buggy plugin exporting only one of the pair (constructible: `reentry_probe.cpp:48`) gets torn/lost live config. REFUTED clean: watchdog slot lifecycle (CAS-claimed, deadline-matched, atomic[64]), cap shadow-stack promote/erase (single mu_, phase-1-before-phase-2, per-owner dedup — N1 transient is the only residual), prepare/commit compliant path. Order O2→O1→O3. **ROUNDS 9 & 10 ALL FIXED @ `9712aab`..`495f220`** (full gate green): O2 dismiss()-after-teardown (app-team); N1 synchronous `sweep_caps_for(owner)` at evict (window→0); O1 `seh_predump_drain_hook` drains a sibling minidump before the SO `_Exit` (closes the H7 hole) + comments corrected; O3 unpaired prepare/commit → warn + degrade to gated path; N2 doc-14 rule 6 (resolve must pin); N3 `min(e.len, remaining())`; N4 fault no longer binds an unbound stream id. **Sweep treated as ongoing, not stamped "closed."** |
