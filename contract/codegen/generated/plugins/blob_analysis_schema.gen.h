#pragma once
// GENERATED by contract/codegen/gen_contract.py from contract/plugins/blob_analysis.decl.json — DO NOT EDIT.
// PackSchema keyset for blob_analysis — the compile-time declared key
// order the offset-accessor read path resolves to slots (docs/new_gen/07).
// enum names ARE the slots; TypedPack<Schema> reads slots_[kX] directly —
// no hash, no scan. Arrays are omitted (they are not flat slots).

#include <xi/xi_pack.hpp>

#include <array>
#include <string_view>

namespace xi::blob {

struct BlobAnalysisSchema : xi::PackSchema<BlobAnalysisSchema> {
    static constexpr std::array<std::string_view, 8> keys = { "gray", "threshold", "min_area", "max_area", "invert", "binary", "blob_count", "threshold_used" };
    enum : int { kGray, kThreshold, kMinArea, kMaxArea, kInvert, kBinary, kBlobCount, kThresholdUsed };
};

}  // namespace xi::blob
