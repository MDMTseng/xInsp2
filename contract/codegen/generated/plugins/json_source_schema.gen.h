#pragma once
// GENERATED by contract/codegen/gen_contract.py from contract/plugins/json_source.decl.json — DO NOT EDIT.
// PackSchema keyset for json_source — the compile-time declared key
// order the offset-accessor read path resolves to slots (docs/new_gen/07).
// enum names ARE the slots; TypedPack<Schema> reads slots_[kX] directly —
// no hash, no scan. Arrays are omitted (they are not flat slots).

#include <xi/xi_pack.hpp>

#include <array>
#include <string_view>

namespace xi::json_source {

struct JsonSourceSchema : xi::PackSchema<JsonSourceSchema> {
    static constexpr std::array<std::string_view, 1> keys = { "seq" };
    enum : int { kSeq };
};

}  // namespace xi::json_source
