#pragma once
// GENERATED by contract/codegen/gen_contract.py from contract/plugins/mock_camera.decl.json — DO NOT EDIT.
// FrameSchema keyset for mock_camera — the compile-time declared key
// order the offset-accessor read path resolves to slots (docs/new_gen/07).
// enum names ARE the slots; TypedFrame<Schema> reads slots_[kX] directly —
// no hash, no scan. Arrays are omitted (they are not flat slots).

#include <xi/xi_frame.hpp>

#include <array>
#include <string_view>

namespace xi::mock_camera {

struct MockCameraSchema : xi::FrameSchema<MockCameraSchema> {
    static constexpr std::array<std::string_view, 1> keys = { "frame" };
    enum : int { kFrame };
};

}  // namespace xi::mock_camera
