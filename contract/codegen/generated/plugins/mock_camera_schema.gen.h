#pragma once
// GENERATED by contract/codegen/gen_contract.py from contract/plugins/mock_camera.decl.json — DO NOT EDIT.
// PackSchema keyset for mock_camera — the compile-time declared key
// order the offset-accessor read path resolves to slots (docs/new_gen/07).
// enum names ARE the slots; TypedPack<Schema> reads slots_[kX] directly —
// no hash, no scan. Arrays are omitted (they are not flat slots).

#include <xi/xi_pack.hpp>

#include <array>
#include <string_view>

namespace xi::mock_camera {

struct MockCameraSchema : xi::PackSchema<MockCameraSchema> {
    static constexpr std::array<std::string_view, 3> keys = { "frame", "seq", "gain" };
    enum : int { kFrame, kSeq, kGain };
};

}  // namespace xi::mock_camera
