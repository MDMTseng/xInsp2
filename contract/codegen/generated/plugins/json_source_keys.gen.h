#pragma once
// GENERATED by contract/codegen/gen_contract.py from contract/plugins/json_source.decl.json — DO NOT EDIT.
// json_source — user-authored JSON injector (open output; typed control surface).
//
// The single source of truth for this plugin's Record key names (guard 1 of
// the plugin data contract). Every string key is named exactly once; the
// builder, the extractor, and the plugin's own reader all compile from it.
// kSchemaVersion bumps on an incompatible input/config change.

namespace xi::json_source {

inline constexpr int kSchemaVersion = 1;

namespace keys {

inline constexpr const char* kPatches = "patches";  // array of {key,value} — per-emit patches (open value; generated Patch builder)
inline constexpr const char* kKey = "key";  // string — patch target path
inline constexpr const char* kData = "data";  // string — the user's JSON document, spliced raw under "data"
inline constexpr const char* kCommand = "command";  // string (command selector)
inline constexpr const char* kValue = "value";  // int (command payload value)
inline constexpr const char* kSetData = "set_data";
inline constexpr const char* kReset = "reset";
inline constexpr const char* kGetStatus = "get_status";
inline constexpr const char* kSeq = "seq";  // int (emit counter; pack-mode entry, mirrors mock_camera). The user document's own fields ride as open, dynamically-keyed pack entries — not declared slots (open output by design).

}  // namespace keys
}  // namespace xi::json_source
