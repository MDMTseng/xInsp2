#pragma once
// GENERATED by contract/codegen/gen_contract.py from contract/plugins/synced_stereo.decl.json — DO NOT EDIT.
// PackSchema keyset for synced_stereo — the compile-time declared key
// order the offset-accessor read path resolves to slots (docs/new_gen/07).
// enum names ARE the slots; TypedPack<Schema> reads slots_[kX] directly —
// no hash, no scan. Arrays are omitted (they are not flat slots).

#include <xi/xi_pack.hpp>

#include <array>
#include <string_view>

namespace xi::synced_stereo {

struct SyncedStereoSchema : xi::PackSchema<SyncedStereoSchema> {
    static constexpr std::array<std::string_view, 3> keys = { "left", "right", "seq" };
    enum : int { kLeft, kRight, kSeq };
};

}  // namespace xi::synced_stereo
