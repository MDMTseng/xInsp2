{
  "$comment": "TRANSCRIBED from plugins/synced_stereo/synced_stereo_keys.h + _io.h. synced_stereo is a GATHERING SOURCE: no process() input, its surface is CONFIG (fps settable; running/ticks read-only get_def), COMMANDS (start/stop/set_fps/fire — fire's n defaults to 1 via the param \"default\"), and a two-image FRAME (left+right under one trigger) whose derived has_both() convenience is the \"frame_composites\" family (polaris2 codegen gap #2 — fully generated, no more \"handwritten_io\"). v12 (THE CUT): the sealed xi.pack@1 Pack is the sole emit currency — left+right+seq gathered into ONE pack per trigger, compiled against kSeq/kLeft/kRight here. (The wave-2 bilingual pack_mode knob was removed once the Record path it toggled was deleted.)",
  "plugin": "synced_stereo",
  "namespace": "xi::synced_stereo",
  "schema_version": 1,
  "role": "source",
  "title": "synced_stereo — correlated left+right frame source (one trigger, two images).",
  "config": [
    { "key": "fps",       "type": "int",  "doc": "int (settable)" },
    { "key": "running",   "type": "bool", "readonly": true, "doc": "bool (get_def only, read-only)" },
    { "key": "ticks",     "type": "int",  "readonly": true, "doc": "int (get_def only, read-only)" }
  ],
  "commands": {
    "selector_key": "command",
    "value_key": "value",
    "list": [
      { "name": "start" },
      { "name": "stop" },
      { "name": "set_fps", "params": [ { "key": "value", "arg": "v", "type": "int" } ] },
      { "name": "fire",    "doc": "Deterministic headless drive: emit `n` frames synchronously.", "params": [ { "key": "n", "arg": "n", "type": "int", "default": 1 } ] }
    ]
  },
  "output_frame": [
    { "key": "left",  "type": "image", "accessor": "left",  "has_accessor": "has_left",  "doc": "image (vertical stripes)" },
    { "key": "right", "type": "image", "accessor": "right", "has_accessor": "has_right", "doc": "image (horizontal stripes)" },
    { "key": "seq",   "type": "int",   "doc": "int (correlation counter; both images carry it, pack-mode canonical entry)" }
  ],
  "frame_composites": [
    { "accessor": "has_both", "all_of": ["left", "right"] }
  ]
}
