#pragma once
//
// xi_types_cv.hpp — [ABI v12 — THE CUT] REDUCED TO A STUB.
//
// This header was the OpenCV interop for the small nominal vector/matrix/Region
// value types declared in xi_types.hpp (to_cv / from_cv / area / bbox). BOTH
// xi_types.hpp and its Typed+Record world were DELETED at THE CUT, so this
// bridge has nothing left to convert and NOTHING in the tree includes it.
//
// The file is kept (empty) rather than removed so any stale `#include
// <xi/xi_types_cv.hpp>` a not-yet-cut consumer still carries resolves to a
// harmless no-op instead of a hard "file not found". It can be deleted outright
// once the tree is confirmed free of references.
//
