<!-- AUTO-GENERATED by tools/gen_backend_map.py — do not edit; regenerate with: python tools/gen_backend_map.py -->

# Backend map

Machine-generated navigation index for coding agents. For the hand-written "find X → do Y" recipes see [docs/agent/NAVIGATION.md](../../docs/agent/NAVIGATION.md).

## ① Translation units & headers

First sentence of each file's top comment (its responsibility).

### backend/src

| File | Responsibility |
| --- | --- |
| `crash_breakpad.cpp` | Breakpad-backed minidump writer. |
| `fe_main.cpp` | xinsp-fe.exe, the frontend supervisor. |
| `runner_main.cpp` | xinsp-runner.exe Headless production runner. |
| `service_cmd_dispatch.cpp` | dispatch-control command handlers, split from service_main.cpp (behavior-preserving; see service_state.hpp / service_cmds.hpp). |
| `service_cmd_observability.cpp` | observability command handlers, split from service_main.cpp (behavior-preserving; see service_state.hpp / service_cmds.hpp). |
| `service_cmd_plugin.cpp` | plugin-mgmt command handlers, split from service_main.cpp (behavior-preserving; see service_state.hpp / service_cmds.hpp). |
| `service_cmd_project.cpp` | project-CRUD command handlers, split from service_main.cpp (behavior-preserving; see service_state.hpp / service_cmds.hpp). |
| `service_cmd_project_lc.cpp` | PROJECT + working-copy lifecycle command handlers: open/close/create/save/load_project and commit/discard_working_copy, plus the small session handlers cmd:ping / cmd:version / cmd:shutdown (generic… |
| `service_cmd_script.cpp` | SCRIPT lifecycle command handler: cmd:compile_and_load (compile-or-load the script .dll, then the run_mu+script_mu hot-swap that makes it the active script) plus the cross-frame state carried across… |
| `service_dispatch.cpp` | per-group dispatch lanes, pool lifecycle, trigger sink, controlled teardown + host-tracked instance state. |
| `service_health.cpp` | the core-owned health/state contract's service surface (adoption-map item 10 / triage Bucket E). |
| `service_inspect.cpp` | the single-frame inspection cycle: COMPUTE half (script invocation + SEH boundary + crash breadcrumb + watchdog) and EMISSION half (metrics + ordered-emit gate + staged flush + result/events), plus… |
| `service_main.cpp` | xinsp-backend.exe entry point (M2 skeleton). |
| `service_result.cpp` | the status registry (set_status_internal / status_cb) and the per-run Result surface (result_cb, trigger_id_hex, outcome_class_for_code, emit_run_result). |
| `service_sinks.cpp` | the script host_api callback surface: pipeline graph capture glue, ordered output sinks (stage/drain/flush), trigger-loop state, trigger-access script callbacks, image-pool owner thunks,… |
| `service_toolchain.cpp` | C++ toolchain resolution + per-project override, script external-dep parsing, project DLL search path, and the instance crash-breadcrumb helpers. |
| `stb_impl.cpp` | Single translation unit that instantiates stb_image_write + stb_image. |
| `xi_image_io.cpp` | Out-of-line installer of the backend's internal image-decode function. |
| `service_cmds.hpp` | PRIVATE command / dispatch layer for the service_*.cpp TUs. |
| `service_guard.hpp` | the fault-boundary templates shared by every backend TU that enters plugin or script-DLL code: guarded_plugin_call (the ONE plugin-entry SEH/exception boundary with the item-14 quarantine/on-fault… |
| `service_state.hpp` | PRIVATE shared ENGINE STATE for the split service_*.cpp TUs. |
| `xi_result_class.hpp` | the ONE home for the run-result reserved band. |

### backend/include/xi

| Header | Responsibility |
| --- | --- |
| `xi.hpp` | umbrella include for xInsp2 inspection routines. |
| `xi_abi.hpp` | C++ wrapper over the stable C plugin ABI. |
| `xi_async.hpp` | parallelism primitives for xInsp2 inspection routines. |
| `xi_atomic_io.hpp` | crash-safe file writes. |
| `xi_b64.hpp` | the ONE base64 encoder (RFC 4648, standard alphabet, '=' pad). |
| `xi_binary_sink.hpp` | indirection so a plugin's host_api->emit_binary() can reach the backend's WS server without xi_core depending on the backend. |
| `xi_blob_head.hpp` | the self-describing blob head format + its ONE validation seam (spec 30, docs/new_gen/30-self-describing-blob-plane.md). |
| `xi_cabi_adapter.hpp` | the primitives for loading and wrapping a SINGLE C-ABI plugin: the ABI-version gate (plugin_abi_compatible), the loaded-plugin descriptor (PluginInfo: manifest fields + DLL handle + factory… |
| `xi_cap_abi.hpp` | the HOST side of the capability plane (docs/new_gen/14, polaris2 pilot): lib plugins register named, pack-door-shaped capabilities; consumers call them by NAME through the host forwarding funnel. |
| `xi_cap_guard.hpp` | the thread-local state behind the capability plane's two gates (docs/new_gen/14, polaris2 pilot). |
| `xi_certify.hpp` | Part III G1: scan / certification isolation. |
| `xi_cli_args.hpp` | command-line / environment argument parsing for the backend entrypoint. |
| `xi_clock.hpp` | the single home for time. |
| `xi_cmake_build.hpp` | host-side cmake invocation for "build": "cmake" plugins. |
| `xi_compress_sink.hpp` | indirection so a plugin's host_api->compress_image() can reach the backend's JPEG encoder without xi_core depending on the codec layer. |
| `xi_config_validate.hpp` | validate an instance's `config` JSON against the plugin's manifest.params declarations (-> OpenWarning diagnostics). |
| `xi_contract.hpp` | the shared data-contract VOCABULARY (reason codes, declared types, and the string-channel fault shape) a plugin's typed-io headers reuse. |
| `xi_crash_dump.hpp` | minidump + crash-report forensics for the compute core. |
| `xi_cv.hpp` | OpenCV convenience helpers with xInsp2's RGB convention baked in. |
| `xi_dynlib.hpp` | cross-platform dynamic-module loader shim. |
| `xi_emit_gate.hpp` | ordered-emit gate for the dispatch lanes. |
| `xi_fault_policy.hpp` | the per-instance POST-FAULT policy vocabulary (adoption-map item 14). |
| `xi_graph_capture.hpp` | opt-in pipeline dataflow-graph capture (stage 2). |
| `xi_health.hpp` | the core-owned health / state contract (schema xi.health/1). |
| `xi_health_mirror.hpp` | the FE health-mirror file write, factored out so it is (a) concurrency-safe and (b) unit-testable without the Engine. |
| `xi_image.hpp` | the Image type for xInsp2 inspection routines. |
| `xi_image_blob.hpp` | the CONSUMER-side reader for the `xi/image` convention type carried in a self-describing Pack blob's descriptor (spec 30, docs/new_gen/30-self-describing-blob-plane.md). |
| `xi_image_blob_mint.hpp` | the PRODUCER-side (write) helpers for the `xi/image` convention type carried in a self-describing Pack blob (spec 30, docs/new_gen/30-self-describing-blob-plane.md). |
| `xi_image_dt.hpp` | the ONE `xi/image` "dt" element-size table (spec 30, doc 28 finding A②). |
| `xi_image_pool.hpp` | host-side refcounted image pool (mostly lock-free). |
| `xi_inflight_runs.hpp` | structural owner for detached one-shot / cmd:run inspect threads. |
| `xi_ingress.hpp` | the domain edge for foreign msgpack (polaris2 wave-1, task 1c). |
| `xi_instance.hpp` | persistent, UI-backed state for xInsp2 inspection routines. |
| `xi_instance_folders.hpp` | name→folder lookup the host_api delegates to. |
| `xi_io.hpp` | per-run context accessors for inspection scripts. |
| `xi_jpeg.hpp` | encode xi::Image to JPEG bytes. |
| `xi_jpeg_cap.hpp` | delegate a preview JPEG encode to the "xi.jpeg.encode" capability (the xi.imgcodec lib plugin), the ENCODE half of the polaris2 CORE-CODEC EVICTION. |
| `xi_json.hpp` | RAII C++ wrapper around yyjson for plugin internals. |
| `xi_json_escape.hpp` | the ONE JSON string-escape primitive. |
| `xi_kv.hpp` | cross-frame script state, post-Record shape (U2 decision, docs/new_gen/16-script-state-shape.md). |
| `xi_log_sink.hpp` | indirection so a plugin/script host_api->log() can reach the backend's operator-visible log channel (WS log + recent-errors ring) without xi_core depending on the backend. |
| `xi_metrics.hpp` | minimal observability export (OQ-7a, core_fix_plan §21 / §27.5). |
| `xi_mp.hpp` | the canonical msgpack codec for the xInsp3 (v3) pack plane. |
| `xi_mp_build.hpp` | sequential, count-free builders and read-views over the canonical msgpack codec (xi_mp.hpp). |
| `xi_owner_lock.hpp` | advisory single-writer stamp for a project folder (F5). |
| `xi_pack.hpp` | the uniform keyed-buffer pack container, SLAB representation, SELF-DESCRIBING BLOB plane (decision ⑤-final, docs/new_gen/30-self-describing-blob-plane.md; supersedes the @3 image/tensor/ type_id… |
| `xi_pack_abi.hpp` | the HOST side of the xi.pack@1 data-plane door (polaris2 wave-2, docs/new_gen/07-uniform-keyed-buffer-plane.md + 08 Wave 2). |
| `xi_pack_contract.hpp` | the pack-plane error-path + provenance contract (polaris2 U1, docs/new_gen/15-pack-fault-semantics.md). |
| `xi_pack_report.hpp` | the report-envelope convention helper (doc 27). |
| `xi_parallel.hpp` | the blessed parallel-pixel-loop wrapper for inspection routines. |
| `xi_param.hpp` | tunable values for xInsp2 inspection routines. |
| `xi_plugin_export.hpp` | package a project plugin as a standalone deployable. |
| `xi_plugin_manager.hpp` | discovers, loads, and manages plugins. |
| `xi_plugin_support.hpp` | convenience header force-included by the project-plugin compile path (CompileMode::PluginDev / PluginExport). |
| `xi_pm_discovery.hpp` | PluginManager plugin discovery / certification / registration / external-plugin resolution. |
| `xi_pm_instances.hpp` | PluginManager instance CRUD, lifecycle state, and project/instance JSON persistence. |
| `xi_pm_json.hpp` | minimal JSON-output helpers shared by the plugin/project machinery. |
| `xi_pm_load.hpp` | PluginManager DLL load / compile / hot-recompile / cmake rebuild / export concern. |
| `xi_pm_manager_core.hpp` | the PluginManager class DECLARATION. |
| `xi_pm_parse.hpp` | JSON parsing helpers for the plugin/project layer: top-level key extraction (extract_string / detail_find_key), a tolerant boolean-flag probe (json_flag_true), and plugin.json manifest parsing… |
| `xi_pm_project.hpp` | PluginManager project lifecycle + working-copy concern. |
| `xi_proc.hpp` | the ONE bounded win32 process spawn (leaf: std + win32 only). |
| `xi_project.hpp` | project.json save/load for xInsp2. |
| `xi_project_model.hpp` | the project DATA MODEL: the plain-old-data structs that describe an opened project (instances, trigger policy, parallelism / dispatch groups, compile environment). |
| `xi_protocol.hpp` | xInsp2 WebSocket protocol types (C++ side). |
| `xi_quiesce_token.hpp` | compile-enforced "dispatch is quiesced" capability. |
| `xi_reactive.hpp` | xi::Derived<Out>: a demand-gated, dedup-memoized derivation cell for SDK-side plugin code. |
| `xi_result.hpp` | let an inspection script publish the ONE per-run result: a signed status code + a human message. |
| `xi_script.hpp` | public ABI between xInsp2 backend and dynamically loaded user scripts. |
| `xi_script_compiler.hpp` | thin wrapper around MSVC cl.exe that compiles a user C++ file into a DLL linked against the xInsp2 core headers. |
| `xi_script_loader.hpp` | LoadLibrary the compiled user script and hold resolved function pointers for the entry and thunks. |
| `xi_script_pack.hpp` | SCRIPT-SIDE Pack building (polaris2 gate P2, docs/new_gen/10-pack-migration-scope.md). |
| `xi_script_support.hpp` | default implementations of the script export thunks described in xi_script.hpp. |
| `xi_seh.hpp` | convert Windows SEH (access violation, divide-by-zero, etc.) into C++ exceptions catchable by ordinary try/catch. |
| `xi_sha256.hpp` | minimal SHA-256 (FIPS 180-4) for tamper-detection. |
| `xi_status.hpp` | let an inspection script publish a short "what am I doing right now" string. |
| `xi_status_sink.hpp` | indirection so a plugin's host_api->set_status() can reach the backend's status registry without xi_core depending on the backend. |
| `xi_test.hpp` | a tiny test framework (~100 lines, zero dependencies). |
| `xi_thread.hpp` | std::thread wrapper that installs SEH translation and catches stray exceptions at thread entry. |
| `xi_toolchain.hpp` | C++ toolchain health + per-project override resolution. |
| `xi_trigger_bus.hpp` | the host-side dispatch funnel for emit_pack. |
| `xi_use.hpp` | script-side proxy to access backend-managed instances. |
| `xi_working_copy.hpp` | filesystem mechanics for transactional working-copy edits (the <project>/.xinsp_work scratch model). |
| `xi_ws_server.hpp` | minimal single-client WebSocket server. |

## ② WS commands → handler → defining file

Parsed from `g_cmd_table` in `backend/src/service_main.cpp`.

| Command | Handler | Defined in |
| --- | --- | --- |
| `clear_crash_reports` | `cmd_clear_crash_reports_` | `backend/src/service_cmd_observability.cpp` |
| `close_project` | `cmd_close_project_` | `backend/src/service_cmd_project_lc.cpp` |
| `commit_group` | `cmd_commit_group_` | `backend/src/service_cmd_dispatch.cpp` |
| `commit_working_copy` | `cmd_commit_working_copy_` | `backend/src/service_cmd_project_lc.cpp` |
| `compile_and_load` | `cmd_compile_and_load_` | `backend/src/service_cmd_script.cpp` |
| `crash_reports` | `cmd_crash_reports_` | `backend/src/service_cmd_observability.cpp` |
| `create_instance` | `cmd_create_instance_` | `backend/src/service_cmd_project.cpp` |
| `create_project` | `cmd_create_project_` | `backend/src/service_cmd_project_lc.cpp` |
| `discard_working_copy` | `cmd_discard_working_copy_` | `backend/src/service_cmd_project_lc.cpp` |
| `dispatch_stats` | `cmd_dispatch_stats_` | `backend/src/service_cmd_observability.cpp` |
| `exchange_instance` | `cmd_exchange_instance_` | `backend/src/service_cmd_dispatch.cpp` |
| `export_project_plugin` | `cmd_export_project_plugin_` | `backend/src/service_cmd_plugin.cpp` |
| `get_dashboard` | `cmd_get_dashboard_` | `backend/src/service_cmd_project.cpp` |
| `get_health` | `cmd_get_health_` | `backend/src/service_health.cpp` |
| `get_instance_def` | `cmd_get_instance_def_` | `backend/src/service_cmd_project.cpp` |
| `get_plugin_ui` | `cmd_get_plugin_ui_` | `backend/src/service_cmd_plugin.cpp` |
| `get_state` | `cmd_get_state_` | `backend/src/service_cmd_observability.cpp` |
| `graph_capture` | `cmd_graph_capture_` | `backend/src/service_cmd_observability.cpp` |
| `graph_snapshot` | `cmd_graph_snapshot_` | `backend/src/service_cmd_observability.cpp` |
| `image_pool_stats` | `cmd_image_pool_stats_` | `backend/src/service_cmd_observability.cpp` |
| `list_instances` | `cmd_list_instances_` | `backend/src/service_cmd_project.cpp` |
| `list_params` | `cmd_list_params_` | `backend/src/service_cmd_project.cpp` |
| `list_plugins` | `cmd_list_plugins_` | `backend/src/service_cmd_plugin.cpp` |
| `load_project` | `cmd_load_project_` | `backend/src/service_cmd_project_lc.cpp` |
| `metrics` | `cmd_metrics_` | `backend/src/service_cmd_observability.cpp` |
| `open_project` | `cmd_open_project_` | `backend/src/service_cmd_project_lc.cpp` |
| `open_project_warnings` | `cmd_open_project_warnings_` | `backend/src/service_cmd_observability.cpp` |
| `ping` | `cmd_ping_` | `backend/src/service_cmd_project_lc.cpp` |
| `prepare_instance` | `cmd_prepare_instance_` | `backend/src/service_cmd_dispatch.cpp` |
| `rebuild_plugins` | `cmd_rebuild_plugins_` | `backend/src/service_cmd_plugin.cpp` |
| `recent_errors` | `cmd_recent_errors_` | `backend/src/service_cmd_observability.cpp` |
| `recompile_project_plugin` | `cmd_recompile_project_plugin_` | `backend/src/service_cmd_plugin.cpp` |
| `remove_instance` | `cmd_remove_instance_` | `backend/src/service_cmd_project.cpp` |
| `rename_instance` | `cmd_rename_instance_` | `backend/src/service_cmd_project.cpp` |
| `rescan_plugins` | `cmd_rescan_plugins_` | `backend/src/service_cmd_plugin.cpp` |
| `run` | `cmd_run_` | `backend/src/service_cmd_dispatch.cpp` |
| `save_instance_config` | `cmd_save_instance_config_` | `backend/src/service_cmd_project.cpp` |
| `save_project` | `cmd_save_project_` | `backend/src/service_cmd_project_lc.cpp` |
| `set_instance_def` | `cmd_set_instance_def_` | `backend/src/service_cmd_project.cpp` |
| `set_param` | `cmd_set_param_` | `backend/src/service_cmd_project.cpp` |
| `set_process_priority` | `cmd_set_process_priority_` | `backend/src/service_cmd_plugin.cpp` |
| `set_timer_fps` | `cmd_set_timer_fps_` | `backend/src/service_cmd_dispatch.cpp` |
| `set_toolchain_override` | `cmd_set_toolchain_override_` | `backend/src/service_cmd_plugin.cpp` |
| `set_watchdog_ms` | `cmd_set_watchdog_ms_` | `backend/src/service_cmd_observability.cpp` |
| `shutdown` | `cmd_shutdown_` | `backend/src/service_cmd_project_lc.cpp` |
| `start` | `cmd_start_` | `backend/src/service_cmd_dispatch.cpp` |
| `status` | `cmd_status_` | `backend/src/service_cmd_observability.cpp` |
| `stop` | `cmd_stop_` | `backend/src/service_cmd_dispatch.cpp` |
| `toolchain_health` | `cmd_toolchain_health_` | `backend/src/service_cmd_plugin.cpp` |
| `version` | `cmd_version_` | `backend/src/service_cmd_project_lc.cpp` |

## ③ Plugins (toolbox/*/plugin.json)

Flags: `lib` = capability provider (no data plane); `autoload` = boot-time eligible; `has_ui` = ships a webUI; `sink` = ordered output sink; `reentrant` = no per-instance serialization.

| Plugin | Flags | Exchange verbs | Capabilities |
| --- | --- | --- | --- |
| `blob_analysis` (toolbox/blob_analysis/plugin.json) | `has_ui` | — | — |
| `cache` (toolbox/cache/plugin.json) | — | — | — |
| `config_swap_probe` (toolbox/config_swap_probe/plugin.json) | — | — | — |
| `controls_demo` (toolbox/controls_demo/plugin.json) | `has_ui` | — | — |
| `data_output` (toolbox/data_output/plugin.json) | `has_ui` | — | — |
| `expose` (toolbox/expose/plugin.json) | `has_ui`, `sink` | `get`, `list_channels`, `subscribe`, `unsubscribe` | — |
| `imgcodec` (toolbox/imgcodec/plugin.json) | `lib`, `autoload`, `reentrant` | `clear_cache`, `stats` | `xi.image.decode`, `xi.jpeg.encode` |
| `json_source` (toolbox/json_source/plugin.json) | `has_ui` | — | — |
| `lut_client` (toolbox/lut_owner/example/plugins/lut_client/plugin.json) | — | — | — |
| `lut_owner` (toolbox/lut_owner/plugin.json) | `lib`, `reentrant` | `recycle_all`, `stats` | `demo.lut.build`, `demo.lut.dump`, `demo.lut.query` |
| `mock_camera` (toolbox/mock_camera/plugin.json) | `has_ui` | — | — |
| `record_replay` (toolbox/record_replay/plugin.json) | — | — | — |
| `record_save` (toolbox/record_save/plugin.json) | `has_ui` | — | — |
| `synced_stereo` (toolbox/synced_stereo/plugin.json) | — | — | — |
| `ui_egress` (toolbox/ui_egress/plugin.json) | `lib`, `autoload`, `reentrant` | `clear`, `stats` | `xi.ui.egress` |

## ④ Pluginlets (toolbox/pluginlets/*/pluginlet.json)

Parasitic UI/logic halves compiled into a host, not standalone plugins (docs/new_gen/37-pluginlet-model.md).

| Pluginlet | Native symbol | UI entry |
| --- | --- | --- |
| `controls` (toolbox/pluginlets/controls/pluginlet.json) | `xi::pluginlet::Controls` | `ui/mount-schema.mjs` |
| `live-view` (toolbox/pluginlets/live-view/pluginlet.json) | `xi::pluginlet::LiveView` | `ui/live-view.ui.ts` |
